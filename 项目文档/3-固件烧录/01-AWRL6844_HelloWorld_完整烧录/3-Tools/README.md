# 🛠️ 烧录工具集

> **AWRL6844固件烧录所需的全部工具**

---

## 工具清单

### 1. arprog_cmdline_6844.exe

**大小**: ~8MB  
**来源**: `MMWAVE_L_SDK_06_01_00_01/tools/FlashingTool/`  
**功能**: 串口烧录工具

**支持操作**:
- ✅ Flash烧录（Programming）
- ✅ Flash读取（Read）
- ✅ Flash擦除（Erase）
- ✅ Flash验证（Verify）

**基本用法**:
```bash
# 烧录文件
arprog_cmdline_6844.exe -p COM3 -f firmware.bin -o 0x100

# 读取Flash
arprog_cmdline_6844.exe -p COM3 -r 0x100 -s 1024 -o output.bin

# 全片擦除
arprog_cmdline_6844.exe -p COM3 -e

# 验证烧录
arprog_cmdline_6844.exe -p COM3 -f firmware.bin -o 0x100 -v
```

**参数说明**:
- `-p COM3`: 串口号
- `-f file.bin`: 要烧录的文件
- `-o 0x100`: Flash偏移地址
- `-r addr`: 读取起始地址
- `-s size`: 读取大小（字节）
- `-e`: 全片擦除
- `-v`: 验证模式

---

### 2. buildImage_creator.exe

**大小**: ~6MB  
**来源**: `MMWAVE_L_SDK_06_01_00_01/tools/MetaImageGen/`  
**功能**: 从.appimage提取Build Images

**作用**:
- 解析.appimage文件
- 提取各核心的可执行镜像（.rig文件）
- 为metaImage_creator准备输入

**用法**:
```bash
buildImage_creator.exe -i input.appimage

# 输出文件（自动生成在temp/目录）：
#   - core1_img.release.rig
#   - core2_img.release.rig
#   - ...
```

**处理流程**:
```
.appimage → buildImage_creator → .rig文件
```

---

### 3. metaImage_creator.exe

**大小**: ~6MB  
**来源**: `MMWAVE_L_SDK_06_01_00_01/tools/MetaImageGen/`  
**功能**: 生成Meta Image

**作用**:
- 读取metaimage_cfg.json配置
- 组合多个.rig文件
- 添加Meta Header
- 生成Flash Header（SBL专用）
- 签名和加密（可选）

**用法**:
```bash
metaImage_creator.exe -config metaimage_cfg.release.json

# 输出文件：
#   - 配置中指定的metaImageFile
#   - 例如：sbl_meta.bin 或 hello_world_meta.bin
```

**生成的Meta Image结构**:
```
Meta Image:
  ├─ Meta Header (~1KB)
  ├─ Build Image 1 (R5F)
  ├─ Build Image 2 (DSP)
  └─ Build Image 3 (RF)
```

---

### 4. flashHeader_creator.exe

**大小**: ~5MB  
**来源**: `MMWAVE_L_SDK_06_01_00_01/tools/MetaImageGen/`  
**功能**: 独立创建Flash Header

**用途**: 
- 用于特殊场景（一般由metaImage_creator自动生成）
- 自定义Flash Header参数

**用法**:
```bash
flashHeader_creator.exe -config flash_header_cfg.json
```

---

## 工具链流程

### 完整Meta Image生成流程

```
┌─────────────────────────────────────────────────┐
│           .appimage文件（源文件）                 │
└──────────────────┬──────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────┐
│  buildImage_creator.exe                         │
│  - 解析.appimage                                │
│  - 提取各核心镜像                                │
└──────────────────┬──────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────┐
│  temp/*.rig文件（中间文件）                      │
│  - R5F镜像                                      │
│  - DSP镜像                                      │
│  - RF镜像                                       │
└──────────────────┬──────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────┐
│  metaImage_creator.exe                          │
│  - 读取配置文件                                  │
│  - 组合所有镜像                                  │
│  - 添加Meta Header                              │
│  - 生成Flash Header (SBL)                      │
│  - 签名/加密（可选）                             │
└──────────────────┬──────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────┐
│  *_meta.bin（最终文件）                         │
│  - 可直接烧录到Flash                            │
└──────────────────┬──────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────┐
│  arprog_cmdline_6844.exe                        │
│  - 通过串口烧录                                  │
│  - 写入QSPI Flash                               │
└─────────────────────────────────────────────────┘
```

---

## 使用示例

### 示例1: 生成并烧录SBL

```bash
cd 1-SBL_Bootloader

# Step 1: 提取Build Images
..\3-Tools\buildImage_creator.exe -i sbl.release.appimage

# Step 2: 生成Meta Image
..\3-Tools\metaImage_creator.exe -config metaimage_cfg.release.json

# Step 3: 烧录到Flash
cd ..\3-Tools
.\arprog_cmdline_6844.exe -p COM3 -f ..\1-SBL_Bootloader\sbl_meta.bin -o 0x100
```

### 示例2: 生成并烧录应用

```bash
cd 2-HelloWorld_App

# Step 1: 提取Build Images
..\3-Tools\buildImage_creator.exe -i hello_world_system.release.appimage

# Step 2: 生成Meta Image
..\3-Tools\metaImage_creator.exe -config metaimage_cfg.release.json

# Step 3: 烧录到Flash
cd ..\3-Tools
.\arprog_cmdline_6844.exe -p COM3 -f ..\2-HelloWorld_App\hello_world_meta.bin -o 0x40000
```

---

## 常见问题

### Q1: 为什么需要两个步骤生成Meta Image？

**A**: 
- **buildImage_creator**: 解析专有格式（.appimage）
- **metaImage_creator**: 组合通用格式（.rig）为Meta Image

这种设计允许：
- 灵活组合不同来源的镜像
- 复用现有的.rig文件
- 支持自定义构建流程

---

### Q2: 可以直接烧录.appimage吗？

**A**: **不可以**。
- .appimage是应用打包格式，不是Flash格式
- 必须转换为Meta Image才能烧录
- SBL才能识别Meta Image格式

---

### Q3: 工具支持Linux/Mac吗？

**A**: 
- 官方工具仅支持Windows (.exe)
- Linux/Mac用户可以：
  - 使用Wine运行
  - 使用虚拟机
  - 使用官方提供的Python脚本（部分SDK版本）

---

### Q4: 烧录失败怎么办？

**A**: 检查清单：
1. 串口是否正确（COM号）
2. SOP开关是否设置为SOP_MODE1 (00)
3. USB驱动是否安装
4. 是否按下复位按钮
5. 其他程序是否占用串口

---

### Q5: 如何验证烧录成功？

**A**: 
```bash
# 方法1: 工具自动验证
arprog_cmdline_6844.exe -p COM3 -f file.bin -o 0x100 -v

# 方法2: 读回验证
arprog_cmdline_6844.exe -p COM3 -r 0x100 -s 1024 -o readback.bin
# 然后手动对比file.bin和readback.bin
```

---

## 工具版本

| 工具 | 版本 | SDK版本 |
|------|------|---------|
| arprog_cmdline_6844 | 3.5 | 06.01.00.01 |
| buildImage_creator | 2.0 | 06.01.00.01 |
| metaImage_creator | 2.0 | 06.01.00.01 |
| flashHeader_creator | 2.0 | 06.01.00.01 |

---

## 性能参考

| 操作 | 耗时 | 备注 |
|------|------|------|
| buildImage_creator | 2-5秒 | 取决于文件大小 |
| metaImage_creator | 3-8秒 | 取决于镜像数量 |
| Flash擦除 | 30秒 | 按Sector擦除 |
| Flash烧录 (130KB) | 45秒 | 115200波特率 |
| Flash烧录 (220KB) | 60秒 | 115200波特率 |
| Flash验证 | 20秒 | 读取并比对 |

---

## 相关文档

- [README.md](../README.md) - 项目概述
- [操作指南.md](../操作指南.md) - 详细步骤
- [5-Scripts/README.md](../5-Scripts/README.md) - 自动化脚本

---

**更新日期**: 2025-12-12  
**SDK版本**: 06.01.00.01
