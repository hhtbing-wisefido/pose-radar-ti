# 固件烧录工具 v1.2.0 - 固件库重构设计

> 📅 日期：2025-12-15
> 🎯 目标：从文件级管理升级到项目级管理
> 📝 状态：设计阶段
> 🔴 **重要修正**：2025-12-15 下午 - 识别策略需修正

---

## 🚨 设计方案修正说明

### ❌ 原设计的错误假设

**错误假设**：所有固件项目都包含 `.syscfg` 文件

**实际情况**：
- **90%的场景**：用户使用**预编译固件**（只有`.appimage`/`.bin`）
- **10%的场景**：开发者使用**源代码项目**（包含`.syscfg`、`.cfg`、源码）

**问题**：原设计只能识别源代码项目，无法支持预编译固件！

### ✅ 修正方案

**核心改变**：**以固件文件为标志**，而不是`.syscfg`

| 识别标志 | 原方案 | 修正方案 |
|---------|-------|---------|
| **必须存在** | `.syscfg`文件 | `.appimage`或`.bin`固件文件 |
| **可选存在** | 固件文件 | `.syscfg`和`.cfg`配置文件 |
| **支持场景** | 仅源代码项目 | 预编译固件 + 源代码项目 |

**逻辑修正**：
```python
# ❌ 错误的识别逻辑
if has_syscfg:
    create_project()  # 会漏掉所有预编译固件

# ✅ 正确的识别逻辑
if has_firmware:
    create_project()
    if has_syscfg:
        link_config_files()  # 配置文件是可选的增强信息
```

**后续说明**：以下设计内容基于原方案，需根据此修正方案重新调整实现细节。

---

## 🎯 重构目标

### 当前问题 (v1.1.3)

- ❌ 只扫描单个固件文件（`.appimage`或 `.bin`）
- ❌ 配置文件（`.syscfg`、`.cfg`）关联不清晰
- ❌ SBL和雷达参数配置无智能推荐
- ❌ 无法体现文件之间的项目关系

### v1.2.0改进

- ✅ **固件扫描**：扫描所有`.appimage`固件文件
- ✅ **智能关联**：自动关联配置文件（如果存在）
- ✅ **推荐系统**：智能推荐SBL和雷达配置
- ✅ **配置持久化**：记住用户选择
- ✅ **一键填充**：自动填充所有必需的烧录文件

---

## 📊 数据结构设计

### FirmwareProject类

```python
class FirmwareProject:
    """固件项目数据类"""
    def __init__(self):
        # === 项目标识 ===
        self.name = ""                    # 项目名称
        self.sdk_source = ""              # 来源SDK
        self.project_path = ""            # 项目根目录
      
        # === 核心文件（必须，在同一目录） ===
        self.app_firmware = None          # 应用固件（.appimage/.bin）
        self.syscfg_file = None           # .syscfg硬件配置
        self.rtos_cfg_file = None         # RTOS .cfg系统配置
      
        # === SBL推荐（可选，可能在其他位置） ===
        self.recommended_sbl = []         # 推荐的SBL列表
        # [{
        #     'path': 'C:/ti/.../sbl.bin',
        #     'source': 'radar_toolbox',
        #     'priority': 1,  # 1=最高优先级
        #     'reason': '与项目在同一目录'
        # }, ...]
        self.selected_sbl = None          # 用户选择的SBL
      
        # === 雷达参数配置（可选，可能在其他位置） ===
        self.recommended_radar_cfg = []   # 推荐的雷达配置列表
        self.selected_radar_cfg = None    # 用户选择的雷达配置
      
        # === 元信息 ===
        self.compatibility = ""           # AWRL6844兼容性说明
        self.description = ""             # 项目功能描述
```

---

## 🔍 扫描策略

### 1. 项目识别

**标志**：`.syscfg`文件的存在
**原因**：CCS项目必定包含 `.syscfg`硬件配置

```python
def scan_projects(self):
    """扫描SDK中的固件项目"""
    for sdk_path in self.sdk_paths:
        # 递归查找包含.syscfg的目录
        for root, dirs, files in os.walk(sdk_path):
            syscfg_files = [f for f in files if f.endswith('.syscfg')]
          
            if syscfg_files:
                # 发现一个项目
                project = self._create_project_from_directory(root)
                if project.app_firmware:  # 必须有应用固件
                    self.projects.append(project)
```

### 2. 项目文件扫描

在项目目录及其子目录中查找：

- ✅ 应用固件：`*.appimage`或 `*.bin`（排除SBL）
- ✅ `.syscfg`配置
- ✅ `.cfg`配置（区分RTOS .cfg和雷达参数.cfg）

```python
def _create_project_from_directory(self, root):
    """从目录创建项目对象"""
    project = FirmwareProject()
    project.project_path = root
    project.syscfg_file = self._find_syscfg(root)
  
    # 扫描固件文件
    for dirpath, dirs, files in os.walk(root):
        for filename in files:
            filepath = os.path.join(dirpath, filename)
          
            # 应用固件（排除SBL）
            if filename.endswith(('.appimage', '.bin')):
                if 'sbl' not in filename.lower():
                    if self._is_compatible(filename):
                        project.app_firmware = filepath
          
            # RTOS配置
            elif filename.endswith('.cfg'):
                if self._is_rtos_cfg(filepath):
                    project.rtos_cfg_file = filepath
  
    # 提取项目信息
    project.name = self._extract_project_name(root)
    project.description = self._get_description(project.name)
  
    return project
```

### 3. SBL推荐策略

**按优先级推荐**：

| 优先级      | 来源            | 说明                  |
| ----------- | --------------- | --------------------- |
| **1** | 项目本地        | 与应用固件在同一目录  |
| **2** | 同一SDK/SBL目录 | 来自同一SDK的标准位置 |
| **3** | 其他SDK/通用SBL | 所有兼容的SBL         |

```python
def recommend_sbl(self, project):
    """为项目推荐SBL"""
    recommendations = []
  
    # 策略1: 项目本地
    local_sbl = glob.glob(os.path.join(project.project_path, '**/sbl*.appimage'), recursive=True)
    for sbl in local_sbl:
        recommendations.append({
            'path': sbl,
            'source': '项目本地',
            'priority': 1,
            'reason': '与项目在同一目录，首选'
        })
  
    # 策略2: 同一SDK
    sdk_root = self._get_sdk_root(project.sdk_source)
    sdk_sbl_dir = os.path.join(sdk_root, 'packages/ti/utils/sbl')
    if os.path.exists(sdk_sbl_dir):
        for sbl in glob.glob(os.path.join(sdk_sbl_dir, '**/*.appimage'), recursive=True):
            recommendations.append({
                'path': sbl,
                'source': project.sdk_source,
                'priority': 2,
                'reason': '来自同一SDK，推荐使用'
            })
  
    # 策略3: 其他SDK通用SBL
    for sdk_path in self.sdk_paths:
        if os.path.basename(sdk_path) != project.sdk_source:
            # 扫描其他SDK的SBL
            for sbl in self._find_sbl_in_sdk(sdk_path):
                recommendations.append({
                    'path': sbl,
                    'source': os.path.basename(sdk_path),
                    'priority': 3,
                    'reason': '通用SBL，兼容使用'
                })
  
    # 排序并去重
    recommendations.sort(key=lambda x: x['priority'])
    return self._deduplicate(recommendations)
```

### 4. 雷达参数推荐策略

| 优先级      | 来源                   | 说明                 |
| ----------- | ---------------------- | -------------------- |
| **1** | 项目根目录/profile.cfg | 项目默认配置         |
| **2** | 项目/config/目录       | 项目配置变体         |
| **3** | SDK示例配置            | SDK提供的参考配置    |
| **4** | 用户自定义目录         | 用户保存的自定义配置 |

```python
def recommend_radar_cfg(self, project):
    """推荐雷达参数配置"""
    recommendations = []
  
    # 策略1: 项目默认
    default_cfg = os.path.join(project.project_path, 'profile.cfg')
    if os.path.exists(default_cfg):
        recommendations.append({
            'path': default_cfg,
            'source': '项目默认',
            'priority': 1,
            'reason': '项目自带配置'
        })
  
    # 策略2: 项目config目录
    config_dir = os.path.join(project.project_path, 'config')
    if os.path.exists(config_dir):
        for cfg in glob.glob(os.path.join(config_dir, '*.cfg')):
            if not self._is_rtos_cfg(cfg):  # 排除RTOS .cfg
                recommendations.append({
                    'path': cfg,
                    'source': '项目配置',
                    'priority': 2,
                    'reason': '项目配置变体'
                })
  
    # 策略3: SDK示例
    sdk_root = self._get_sdk_root(project.sdk_source)
    profiles_dir = os.path.join(sdk_root, 'packages/ti/demo/xwr68xx/profiles')
    if os.path.exists(profiles_dir):
        for cfg in glob.glob(os.path.join(profiles_dir, '*.cfg')):
            recommendations.append({
                'path': cfg,
                'source': 'SDK示例',
                'priority': 3,
                'reason': 'SDK参考配置'
            })
  
    # 策略4: 用户自定义
    user_cfg_dir = "D:/radar_configs"  # 可配置路径
    if os.path.exists(user_cfg_dir):
        for cfg in glob.glob(os.path.join(user_cfg_dir, '*.cfg')):
            recommendations.append({
                'path': cfg,
                'source': '用户自定义',
                'priority': 4,
                'reason': '自定义配置'
            })
  
    recommendations.sort(key=lambda x: x['priority'])
    return recommendations
```

---

## 🎨 UI设计

### 整体布局

```
┌─────────────────────────────────────────────────────────────┐
│  📚 固件库 - 项目级管理 (v1.2.0)  [🔄重新扫描] [📂SDK目录] │
├──────────────────┬──────────────────────────────────────────┤
│  📁 固件项目     │  📋 项目详细信息                        │
│  (30%)           │  (70%)                                   │
│  ┌────────────┐  │  ┌────────────────────────────────────┐ │
│  │ 📁 3D人员  │  │  │ 📦 项目信息                        │ │
│  │    追踪    │  │  │  名称: 3D人员追踪                  │ │
│  │            │  │  │  SDK: radar_toolbox                │ │
│  │ 📁 车内检测│  │  │  路径: C:\ti\...                   │ │
│  │            │  │  │                                    │ │
│  │ 📁 手势识别│  │  │ 🔧 固件文件 (自动检测)             │ │
│  │            │  │  │  ✅ 应用: app.appimage             │ │
│  │ ...        │  │  │  ✅ SysConfig: demo.syscfg         │ │
│  │            │  │  │  ✅ RTOS配置: demo.cfg             │ │
│  │            │  │  │                                    │ │
│  │            │  │  │ ───────────────────────────────── │ │
│  │            │  │  │                                    │ │
│  │            │  │  │ 🔧 SBL固件 (选择一个)              │ │
│  │            │  │  │  ○ [推荐] sbl.appimage             │ │
│  │            │  │  │     └─ [项目本地] 首选             │ │
│  │            │  │  │  ○ sbl_lite.appimage               │ │
│  │            │  │  │     └─ [radar_toolbox] 同SDK      │ │
│  │            │  │  │  ○ [手动选择...]                   │ │
│  │            │  │  │                                    │ │
│  │            │  │  │ 📡 雷达参数配置 (可选)             │ │
│  │            │  │  │  ☑ 使用默认配置                    │ │
│  │            │  │  │  ○ profile_3d.cfg                  │ │
│  │            │  │  │     └─ [项目默认]                  │ │
│  │            │  │  │  ○ [手动选择...] [新建...]        │ │
│  │            │  │  │                                    │ │
│  │            │  │  │ [📥烧录] [📂打开] [💾保存]        │ │
│  └────────────┘  │  └────────────────────────────────────┘ │
└──────────────────┴──────────────────────────────────────────┘
```

### 关键UI组件

#### 1. 项目列表（左侧30%）

```python
# Listbox显示格式
"📁 3D人员追踪 [radar_toolbox]"
"📁 车内检测 [MMWAVE_L_SDK]"
"📁 手势识别 [radar_academy]"
```

#### 2. 项目详情（右侧70%）

**使用Canvas+Frame实现滚动**，包含：

- 📦 项目基本信息
- 🔧 核心文件列表（只读）
- 🔧 SBL选择（单选框）
- 📡 雷达配置选择（可选）
- 操作按钮

---

## 💾 配置持久化

### 保存用户选择

```python
# 保存到项目目录/.flash_tool_config.json
{
    "version": "1.2.0",
    "project_path": "C:/ti/.../3d_tracking",
    "selected_sbl": "C:/ti/.../sbl.appimage",
    "selected_radar_cfg": "C:/ti/.../profile.cfg",
    "last_used": "2025-12-15T10:30:00"
}
```

### 加载配置

打开项目时自动加载上次的选择。

---

## 🎯 核心功能实现

### 1. 扫描项目

```python
def scan_projects(self):
    """扫描所有SDK中的固件项目"""
    self.projects = []
  
    for sdk_path in self.sdk_paths:
        if not os.path.exists(sdk_path):
            continue
      
        # 查找包含.syscfg的目录（项目标志）
        for root, dirs, files in os.walk(sdk_path):
            if any(f.endswith('.syscfg') for f in files):
                project = self._create_project(root, sdk_path)
                if project.app_firmware:
                    # 推荐SBL和雷达配置
                    project.recommended_sbl = self.recommend_sbl(project)
                    project.recommended_radar_cfg = self.recommend_radar_cfg(project)
                  
                    # 加载保存的配置
                    self._load_project_config(project)
                  
                    self.projects.append(project)
  
    # 更新UI
    self.update_project_list()
```

### 2. 项目选择

```python
def on_project_select(self, event):
    """用户选择项目"""
    selection = self.project_listbox.curselection()
    if not selection:
        return
  
    index = selection[0]
    self.current_project = self.projects[index]
  
    # 显示项目详情
    self.show_project_details(self.current_project)
```

### 3. 显示项目详情

```python
def show_project_details(self, project):
    """显示项目详细信息"""
    # 清空右侧面板
    for widget in self.detail_frame.winfo_children():
        widget.destroy()
  
    # === 项目信息 ===
    self._add_section("📦 项目信息", [
        f"名称: {project.name}",
        f"SDK: {project.sdk_source}",
        f"路径: {project.project_path}"
    ])
  
    # === 核心文件 ===
    self._add_section("🔧 固件文件 (自动检测)", [
        f"✅ 应用: {os.path.basename(project.app_firmware)}",
        f"✅ SysConfig: {os.path.basename(project.syscfg_file)}",
        f"✅ RTOS配置: {os.path.basename(project.rtos_cfg_file)}"
    ], readonly=True)
  
    self._add_separator()
  
    # === SBL选择 ===
    self._add_sbl_selection(project)
  
    self._add_separator()
  
    # === 雷达配置 ===
    self._add_radar_cfg_selection(project)
  
    self._add_separator()
  
    # === 操作按钮 ===
    self._add_action_buttons(project)
```

### 4. 一键烧录

```python
def flash_project(self, project):
    """烧录项目固件"""
    # 获取选择的SBL
    sbl = self.sbl_var.get()
    if not sbl:
        messagebox.showwarning("提示", "请选择SBL固件")
        return
  
    # 保存配置
    project.selected_sbl = sbl
    if not self.use_default_cfg.get():
        project.selected_radar_cfg = self.radar_cfg_var.get()
    self._save_project_config(project)
  
    # 填充到基本烧录页
    self.app.set_firmware_files(
        sbl=sbl,
        app=project.app_firmware
    )
  
    # 切换到基本烧录页
    self.app.notebook.select(0)
  
    # 询问是否立即烧录
    if messagebox.askyesno("确认", "文件已填充\n是否立即烧录?"):
        self.app.start_flash()
```

---

## ✅ 测试计划

### 1. 项目扫描测试

- [ ] 能否正确识别包含 `.syscfg`的项目
- [ ] 能否找到应用固件（`.appimage`）
- [ ] 能否找到配置文件（`.syscfg`, `.cfg`）
- [ ] 排除非68xx系列的项目

### 2. SBL推荐测试

- [ ] 优先级1：项目本地SBL是否排在首位
- [ ] 优先级2：同SDK的SBL是否正确推荐
- [ ] 优先级3：其他SDK的SBL是否可用
- [ ] 手动选择功能是否正常

### 3. 雷达配置推荐测试

- [ ] 项目默认配置是否优先显示
- [ ] 能否区分RTOS .cfg和雷达参数.cfg
- [ ] 手动选择和新建功能是否正常

### 4. 一键烧录测试

- [ ] 能否正确填充SBL和应用固件
- [ ] 能否正确切换到基本烧录页
- [ ] 配置是否正确保存和加载

---

## 📝 版本迭代计划

### v1.2.0 - 第一阶段（核心功能）

- ✅ 数据结构重构（FirmwareProject类）
- ✅ 项目级扫描（以 `.syscfg`为标志）
- ✅ UI重构（30%/70%布局）
- ✅ SBL推荐系统（3级优先级）
- ✅ 基本的一键烧录

### v1.2.1 - 第二阶段（增强功能）

- ⭐ 雷达配置推荐和选择
- ⭐ 配置持久化（保存/加载用户选择）
- ⭐ 文件查看器（只读查看 `.syscfg`和 `.cfg`）

### v1.2.2 - 第三阶段（优化功能）

- 🔧 项目筛选和搜索
- 🔧 批量操作支持
- 🔧 配置文件编辑器
- 🔧 项目导出和分享

---

## 🚀 实现优先级

**立即实现**（v1.2.0核心）：

1. FirmwareProject数据类
2. 项目扫描逻辑（scan_projects）
3. UI布局重构（30%/70%）
4. 项目列表显示
5. 项目详情显示（基本信息+核心文件）
6. SBL推荐系统（3级优先级）
7. 一键烧录功能

**后续实现**（v1.2.1+）：
8. 雷达配置推荐
9. 配置持久化
10. 文件查看器
11. 高级功能

---

## 💡 关键技术点

### 1. 区分两种.cfg文件

```python
def _is_rtos_cfg(self, filepath):
    """判断是否是RTOS .cfg（而非雷达参数.cfg）"""
    try:
        with open(filepath, 'r', encoding='utf-8') as f:
            content = f.read(500)  # 只读前500字符
            # RTOS .cfg特征：JavaScript语法
            if 'var ' in content or 'xdc.useModule' in content:
                return True
            # 雷达参数.cfg特征：纯命令格式
            if 'sensorStart' in content or 'profileCfg' in content:
                return False
    except:
        pass
    return True  # 默认认为是RTOS .cfg
```

### 2. 项目名称提取

```python
def _extract_project_name(self, path):
    """从路径提取有意义的项目名"""
    parts = path.replace('\\', '/').split('/')
    # 排除编译输出目录
    exclude = ['Debug', 'Release', 'build', 'out', 
               'prebuilt_binaries', 'ti-arm-clang']
  
    for part in reversed(parts):
        if part and part not in exclude:
            return part
    return "Unknown"
```

### 3. 兼容性检查

```python
def _is_compatible(self, filename):
    """检查是否兼容AWRL6844"""
    patterns = ['6844', 'l6844', '68xx', 'xwrl68', 'awrl68']
    return any(p in filename.lower() for p in patterns)
```

---

## 📊 预期效果

### 用户体验改进

- ✅ 从"文件列表"升级到"项目列表" - 更直观
- ✅ 自动关联配置文件 - 更清晰
- ✅ 智能推荐SBL - 更省心
- ✅ 一键烧录 - 更快捷

### 开发者体验

- ✅ 更清晰的代码结构
- ✅ 可扩展的推荐系统
- ✅ 易于维护的UI组件
- ✅ 完善的配置管理

---

**作者**: Benson@Wisefido
**项目**: AWRL6844EVM 姿态检测雷达
**版本**: v1.2.0 设计文档
