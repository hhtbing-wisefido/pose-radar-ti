# AWRL6844 文档修复执行计划

**创建日期**: 2025-12-15  
**目标**: 修正项目文档中所有与官方SDK不符的错误内容  
**基准**: `temp/官方SDK完整流程-2025-12-15.md`  
**对比**: `temp/错误对比分析-2025-12-15.md`

---

## 📋 执行检查清单

### ✅ 准备阶段（已完成）

- [x] 收集官方SDK文档证据
- [x] 分析sbl.h源码定义
- [x] 对比现有文档错误
- [x] 创建官方流程文档
- [x] 创建错误对比文档
- [x] 制定修复计划（本文档）

### 🔄 执行阶段（待进行）

#### P0 - 关键脚本修复（优先）

- [ ] **P0.1** 修复 `5-Scripts/full_flash.bat`
  - 删除Meta生成调用
  - 改用官方 `-cf` 一次性烧录命令
  - 添加详细注释说明

- [ ] **P0.2** 修复 `5-Scripts/3_flash_sbl.bat`
  - 修正arprog参数格式
  - 使用 `-f1 -of1 -s SFLASH -c`

- [ ] **P0.3** 修复 `5-Scripts/4_flash_app.bat`
  - 修正arprog参数格式
  - 使用 `-f1 -of1 -s SFLASH -c`

- [ ] **P0.4** 修复 `操作指南.md`
  - 删除"Phase 1: 准备Meta Images"
  - 删除buildImage_creator/metaImage_creator相关内容
  - 重组为3个Phase: 准备→烧录→验证
  - 更新所有烧录命令为官方格式

#### P1 - 核心文档修复（重要）

- [ ] **P1.1** 修复 `README.md`
  - 删除"方式1/2"中的Meta生成步骤
  - 简化"Phase 1: 准备Meta Images" → "检查文件"
  - 更新"Phase 2/3"为官方烧录命令
  - 更新目录结构说明（4-Generated改为非必需）

- [ ] **P1.2** 删除错误脚本
  - 删除 `5-Scripts/1_generate_sbl_meta.bat`
  - 删除 `5-Scripts/2_generate_app_meta.bat`
  - 更新 `5-Scripts/README.md` 说明

- [ ] **P1.3** 修复 `1-SBL_Bootloader/README.md`
  - 删除buildImage_creator使用说明
  - 明确".appimage可直接烧录"
  - 删除"meta生成"相关章节

- [ ] **P1.4** 修复 `2-HelloWorld_App/README.md`
  - 删除metaImage_creator使用说明
  - 明确".appimage可直接烧录"
  - 删除"meta生成"相关章节

- [ ] **P1.5** 修复 `3-Tools/README.md`
  - 删除buildImage_creator的"必需"标记
  - 删除metaImage_creator的"必需"标记
  - 明确这些工具仅用于"开发调试"，非烧录必需

#### P2 - 细节完善（后续）

- [ ] **P2.1** 修复 `Flash布局说明.md`
  - 更正"~248KB SBL" → "256KB预留空间(实际~130KB)"
  - 添加官方源码引用
  - 强调地址来自sbl.h

- [ ] **P2.2** 更新 `4-Generated/README.md`
  - 标记此目录为"可选/调试用"
  - 说明官方流程不生成这些文件
  - 保留作为历史记录

- [ ] **P2.3** 创建快速参考卡片
  - 新建 `快速开始.md`
  - 仅包含最简化的3步流程
  - 大字体、清晰命令

### ✅ 验证阶段（最后）

- [ ] **V1** 文档一致性检查
  - 全局搜索"buildImage_creator" → 应仅出现在"调试工具"章节
  - 全局搜索"metaImage_creator" → 同上
  - 全局搜索"0x42000" → 确认所有地址一致
  - 全局搜索"arprog" → 确认参数格式统一

- [ ] **V2** 实际测试验证
  - 按修复后的`操作指南.md`执行烧录
  - 验证full_flash.bat正常工作
  - 记录实际串口输出
  - 截图保存成功日志

- [ ] **V3** Git提交
  - 提交修复后的文档
  - 提交message: "fix: 对齐官方SDK烧录流程，删除冗余Meta生成步骤"

---

## 📝 详细修复内容

### 修复 P0.1: full_flash.bat

**当前错误内容**:
```batch
@echo off
...
call 1_generate_sbl_meta.bat
call 2_generate_app_meta.bat
call 3_flash_sbl.bat %COM_PORT%
call 4_flash_app.bat %COM_PORT%
```

**修复后内容**:
```batch
@echo off
setlocal EnableDelayedExpansion

REM ============================================
REM AWRL6844 完整烧录脚本（官方SDK标准流程）
REM 基于: MMWAVE_L_SDK_06_01_00_01官方文档
REM 一次性烧录SBL和Application到QSPI Flash
REM ============================================

set COM_PORT=%1
if "%COM_PORT%"=="" (
    echo [错误] 请指定COM端口
    echo 用法: full_flash.bat COM3
    exit /b 1
)

echo.
echo ============================================
echo   AWRL6844 完整烧录流程
echo ============================================
echo   COM端口: %COM_PORT%
echo   SBL文件: 1-SBL_Bootloader\sbl.release.appimage
echo   App文件: 2-HelloWorld_App\hello_world_system.release.appimage
echo ============================================
echo.

REM 检查文件存在
if not exist "..\1-SBL_Bootloader\sbl.release.appimage" (
    echo [错误] SBL文件不存在！
    exit /b 1
)
if not exist "..\2-HelloWorld_App\hello_world_system.release.appimage" (
    echo [错误] Application文件不存在！
    exit /b 1
)

echo [1/3] 检查烧录工具...
if not exist "..\3-Tools\arprog_cmdline_6844.exe" (
    echo [错误] 烧录工具不存在！
    exit /b 1
)
echo [OK] 烧录工具就绪

echo.
echo [2/3] 执行烧录...
echo.
echo 提示：请确认开发板已设置为SOP_MODE1 (SOP0=OFF, SOP1=OFF)
echo 按任意键继续烧录，或Ctrl+C取消...
pause >nul

REM 使用官方标准命令：-cf参数自动创建Flash头
..\3-Tools\arprog_cmdline_6844.exe -p %COM_PORT% ^
  -f1 "..\1-SBL_Bootloader\sbl.release.appimage" ^
  -f2 "..\2-HelloWorld_App\hello_world_system.release.appimage" ^
  -s SFLASH -c -cf

if %ERRORLEVEL% NEQ 0 (
    echo.
    echo [失败] 烧录出错！错误代码: %ERRORLEVEL%
    exit /b %ERRORLEVEL%
)

echo.
echo [3/3] 烧录完成！
echo.
echo ============================================
echo   下一步操作：
echo   1. 断开EVM电源
echo   2. 设置SOP_MODE2 (SOP0=OFF, SOP1=ON)
echo   3. 连接串口终端 (115200 8N1)
echo   4. 重新上电启动
echo ============================================
echo.

endlocal
```

---

### 修复 P0.2: 3_flash_sbl.bat

**修复后内容**:
```batch
@echo off
setlocal

REM 烧录SBL到Flash 0x2000 (8192字节偏移)

set COM_PORT=%1
if "%COM_PORT%"=="" (
    echo [错误] 请指定COM端口
    echo 用法: 3_flash_sbl.bat COM3
    exit /b 1
)

echo.
echo ============================================
echo   烧录SBL Bootloader
echo ============================================
echo   COM端口: %COM_PORT%
echo   目标地址: 0x2000 (8192)
echo   文件: sbl.release.appimage
echo ============================================
echo.

if not exist "..\1-SBL_Bootloader\sbl.release.appimage" (
    echo [错误] SBL文件不存在！
    exit /b 1
)

echo 提示：请确认SOP_MODE1 (SOP0=OFF, SOP1=OFF)
pause

REM 官方标准命令：手动指定Flash偏移
..\3-Tools\arprog_cmdline_6844.exe -p %COM_PORT% ^
  -f1 "..\1-SBL_Bootloader\sbl.release.appimage" ^
  -of1 8192 ^
  -s SFLASH -c

if %ERRORLEVEL% NEQ 0 (
    echo [失败] SBL烧录出错！
    exit /b %ERRORLEVEL%
)

echo.
echo [完成] SBL烧录成功！
echo.

endlocal
```

---

### 修复 P0.3: 4_flash_app.bat

**修复后内容**:
```batch
@echo off
setlocal

REM 烧录Application到Flash 0x42000 (270336字节偏移)

set COM_PORT=%1
if "%COM_PORT%"=="" (
    echo [错误] 请指定COM端口
    echo 用法: 4_flash_app.bat COM3
    exit /b 1
)

echo.
echo ============================================
echo   烧录HelloWorld应用
echo ============================================
echo   COM端口: %COM_PORT%
echo   目标地址: 0x42000 (270336)
echo   文件: hello_world_system.release.appimage
echo ============================================
echo.

if not exist "..\2-HelloWorld_App\hello_world_system.release.appimage" (
    echo [错误] Application文件不存在！
    exit /b 1
)

echo 提示：请确认SOP_MODE1 (SOP0=OFF, SOP1=OFF)
pause

REM 官方标准命令：手动指定Flash偏移
..\3-Tools\arprog_cmdline_6844.exe -p %COM_PORT% ^
  -f1 "..\2-HelloWorld_App\hello_world_system.release.appimage" ^
  -of1 270336 ^
  -s SFLASH -c

if %ERRORLEVEL% NEQ 0 (
    echo [失败] Application烧录出错！
    exit /b %ERRORLEVEL%
)

echo.
echo [完成] Application烧录成功！
echo 请切换到SOP_MODE2并复位开发板。
echo.

endlocal
```

---

### 修复 P0.4: 操作指南.md（主要章节重写）

**删除的章节**:
- 整个 "Phase 1: 准备Meta Images"
- "Step 1: 提取核心镜像"
- "Step 2: 创建Meta Image"
- 所有buildImage_creator/metaImage_creator相关内容

**新增/重写的章节**:

```markdown
## Phase 1: 硬件连接与文件检查

### 1.1 检查开发板连接

**硬件检查**:
1. USB连接到XDS110端口
2. 查看设备管理器识别的COM口
   - "XDS110 Class Application/User UART" (用于烧录，如COM3)
   - "XDS110 Class Auxiliary Data Port" (用于调试，如COM4)

### 1.2 确认固件文件

确认以下文件存在（SDK预编译，可直接烧录）:

```
✅ 1-SBL_Bootloader/sbl.release.appimage              (~130KB)
✅ 2-HelloWorld_App/hello_world_system.release.appimage (~200KB)
```

**说明**: `.appimage` 文件由SDK编译生成，包含完整的Meta Header和代码，**可直接烧录到Flash**，无需任何中间处理步骤。

---

## Phase 2: 烧录固件到Flash

### 2.1 设置烧录模式

**SOP开关设置**:
- SOP0 = OFF
- SOP1 = OFF
- 模式 = SOP_MODE1 (Flashing Mode)

### 2.2 执行烧录

**方法1: 一键完整烧录（推荐）**

```bash
cd 5-Scripts
full_flash.bat COM3
```

此命令会一次性烧录SBL和Application到正确的Flash地址。

**方法2: 分步烧录**

```bash
cd 5-Scripts

# 烧录SBL到0x2000
3_flash_sbl.bat COM3

# 烧录Application到0x42000
4_flash_app.bat COM3
```

### 2.3 烧录命令详解

**实际执行的命令** (full_flash.bat内部):

```bash
arprog_cmdline_6844.exe -p COM3 \
  -f1 "1-SBL_Bootloader\sbl.release.appimage" \
  -f2 "2-HelloWorld_App\hello_world_system.release.appimage" \
  -s SFLASH -c -cf
```

**参数说明**:
- `-p COM3` - 指定串口
- `-f1` / `-f2` - 指定要烧录的文件（支持最多-f1到-f8）
- `-s SFLASH` - 目标存储器（QSPI Flash）
- `-c` - 初始化时发送break信号
- `-cf` - **自动创建Flash头**（工具自动计算地址和大小）

**烧录过程输出**:

```
SW version: 0.8
Connecting Com Port....
connect to device
INFO : Port Opened
set break signal
--- please restart the device ---
connection succeeded
downloading [0] size [80]
...
[========================================>] 100%
Flash complete!
```

---

## Phase 3: 验证运行

### 3.1 切换运行模式

**SOP开关设置**:
- SOP0 = OFF
- SOP1 = ON
- 模式 = SOP_MODE2 (Functional Mode)

### 3.2 复位启动

1. 断开EVM电源
2. 重新上电
3. 或按"RESET_SW"按钮

### 3.3 查看串口输出

**串口配置**: 115200 8N1

**预期输出**:

```
***** SBL Starting *****
SBL Version: 1.0.0
Loading Application from 0x42000...
Application Size: 204800 bytes
CRC Check: PASS
Meta Header Validation: PASS
Loading R5F Core Image...
Loading C66x DSP Image...
Jumping to Application...

========================================
   AWRL6844 HelloWorld Demo
========================================
Hello World from R5F Core!
DSP Core initialized successfully.
System running...
```

### 3.4 验证成功标志

- ✅ 串口输出"Hello World"
- ✅ 无CRC错误或加载失败信息
- ✅ 多核启动成功
```

---

## 🔍 修复验证方法

### 自动化检查脚本

创建 `verify_docs.ps1`:

```powershell
# 文档修复验证脚本

Write-Host "检查文档修复情况..." -ForegroundColor Cyan

$errors = 0

# 检查1: 不应出现buildImage_creator（除了工具说明）
$buildImageCount = (Select-String -Path "项目文档\3-固件工具\**\*.md" -Pattern "buildImage_creator" -Exclude "3-Tools\README.md").Count
if ($buildImageCount -gt 0) {
    Write-Host "[错误] 仍有 $buildImageCount 处提到 buildImage_creator（工具说明除外）" -ForegroundColor Red
    $errors++
}

# 检查2: 不应出现metaImage_creator（除了工具说明）
$metaImageCount = (Select-String -Path "项目文档\3-固件工具\**\*.md" -Pattern "metaImage_creator" -Exclude "3-Tools\README.md").Count
if ($metaImageCount -gt 0) {
    Write-Host "[错误] 仍有 $metaImageCount 处提到 metaImage_creator（工具说明除外）" -ForegroundColor Red
    $errors++
}

# 检查3: arprog命令格式
$wrongArprog = Select-String -Path "项目文档\3-固件工具\**\*.{md,bat}" -Pattern "arprog.*-o\s+0x"
if ($wrongArprog) {
    Write-Host "[错误] 发现使用 -o 参数的旧格式命令" -ForegroundColor Red
    $wrongArprog | ForEach-Object { Write-Host "  $($_.Filename):$($_.LineNumber)" }
    $errors++
}

# 检查4: 确认使用 -cf 或 -of1
$correctArprog = Select-String -Path "项目文档\3-固件工具\**\*.bat" -Pattern "arprog.*(-cf|-of1)"
if (-not $correctArprog) {
    Write-Host "[警告] 未找到使用 -cf 或 -of1 的命令" -ForegroundColor Yellow
}

# 检查5: 脚本文件不应存在
if (Test-Path "项目文档\3-固件工具\*\5-Scripts\1_generate_sbl_meta.bat") {
    Write-Host "[错误] 1_generate_sbl_meta.bat 应该被删除" -ForegroundColor Red
    $errors++
}
if (Test-Path "项目文档\3-固件工具\*\5-Scripts\2_generate_app_meta.bat") {
    Write-Host "[错误] 2_generate_app_meta.bat 应该被删除" -ForegroundColor Red
    $errors++
}

Write-Host "`n检查完成！" -ForegroundColor Cyan
if ($errors -eq 0) {
    Write-Host "✅ 所有检查通过！" -ForegroundColor Green
} else {
    Write-Host "❌ 发现 $errors 个问题需要修复" -ForegroundColor Red
}
```

---

## 📅 执行时间表

| 阶段 | 任务 | 预计时间 | 负责人 |
|------|------|---------|--------|
| P0 | 修复关键脚本 | 30分钟 | AI |
| P1 | 修复核心文档 | 1小时 | AI |
| P2 | 完善细节 | 30分钟 | AI |
| V1 | 文档验证 | 15分钟 | AI |
| V2 | 实际测试 | 30分钟 | 用户+AI |
| V3 | Git提交 | 10分钟 | 用户 |

**总计**: 约3小时

---

## ✅ 完成标志

修复完成的判断标准：

1. ✅ 所有烧录脚本使用官方标准参数格式
2. ✅ 文档中删除所有"生成Meta"相关步骤
3. ✅ 操作指南简化为3个Phase
4. ✅ README快速开始不超过5步
5. ✅ 全局搜索无误用buildImage/metaImage（调试说明除外）
6. ✅ 实际烧录测试成功

---

**计划创建时间**: 2025-12-15  
**计划状态**: ✅ 已完成  
**下一步**: 开始执行P0修复任务
