# TI SDK 固件目录与文件命名规律分析

**生成时间**: 2025-12-17  
**数据来源**: 2025-12-17_TI_SDK固件列表.md  
**分析样本**: 354个.appimage固件文件  
**SDK版本**: MMWAVE_L_SDK_06_01_00_01, radar_toolbox_3_30_00_06

---

## 📊 一、目录结构规律

### 1.1 MMWAVE_L_SDK 目录结构（282个固件）

#### 标准目录层级（7层）

```
C:/ti/MMWAVE_L_SDK_06_01_00_01/
└── examples/                           ← 第2层：示例根目录
    └── [功能类别]/                     ← 第3层：功能分类
        └── [具体功能]/                 ← 第4层：具体示例名称
            └── xwrL684x-evm/           ← 第5层：硬件平台
                └── [处理器_操作系统]/  ← 第6层：运行配置
                    └── ti-arm-clang/   ← 第7层：编译器
                        └── [固件文件].appimage
```

#### 第3层：功能类别分类（共8大类）

| 类别 | 目录名 | 说明 | 固件数量 |
|-----|--------|------|---------|
| **控制** | `control/` | 雷达控制相关 | ~4 |
| **数据处理** | `datapath/` | 信号处理链路 | ~8 |
| **驱动** | `drivers/` | 硬件驱动示例 | ~240 |
| **基础** | `empty/` | 空白工程模板 | ~4 |
| **Hello World** | `hello_world/` | 基础示例 | ~4 |
| **内核** | `kernel/` | 操作系统示例 | ~8 |
| **雷达Demo** | `mmw_demo/` | 毫米波雷达Demo | ~2 |
| **系统** | `system/` | 系统级配置 | ~12 |

#### 第4层：具体功能子分类

**drivers/ 驱动类别细分（最庞大）**：

```
drivers/
├── boot/               ← 启动加载（SBL、SPI启动）
│   ├── sbl/
│   ├── sbl_lite/
│   └── spibooting/
├── cbuff/              ← 循环缓冲区
├── crc/                ← 循环冗余校验
├── edma/               ← DMA数据传输
│   ├── edma_chain_transfer/
│   ├── edma_interrupt_transfer/
│   ├── edma_link_transfer/
│   └── edma_polled_transfer/
├── epwm/               ← PWM波形生成
├── gpio/               ← 通用IO
│   ├── gpio_input_interrupt/
│   └── gpio_led_blink/
├── hwa/                ← 硬件加速器
│   ├── hwa_cfar_ca/
│   ├── hwa_cfar_os/
│   ├── hwa_compression/
│   ├── hwa_dc_sub/
│   └── hwa_real_fft/
├── i2c/                ← I2C通信
│   ├── i2c_read/
│   └── i2c_temperature/
├── ipc/                ← 核间通信
│   └── ipc_notify_echo/
├── lin/                ← LIN总线
│   ├── lin_external/
│   ├── lin_loopback_interrupts/
│   ├── lin_loopback_polling/
│   ├── lin_sci_dma/
│   └── lin_sci_loopback/
├── mcan/               ← CAN总线
│   ├── mcan_external_read_write/
│   ├── mcan_loopback_interrupt/
│   └── mcan_loopback_polling/
├── mcspi/              ← SPI通信
│   ├── mcspi_loopback/
│   └── mcspi_loopback_dma/
├── pmic/               ← 电源管理
├── power/              ← 电源模式
├── qspi/               ← Quad-SPI Flash
│   ├── qspi_flash_diag/
│   ├── qspi_flash_dma_transfer/
│   └── qspi_flash_transfer/
├── rti/                ← 实时中断定时器
├── sharedmemory/       ← 共享内存
├── soc/                ← 系统级
│   └── warmreset/
├── uart/               ← 串口通信
│   ├── uart_echo/
│   ├── uart_echo_callback/
│   └── uart_echo_dma/
└── watchdog/           ← 看门狗
    ├── watchdog_interrupt/
    └── watchdog_reset/
```

**datapath/ 数据处理类别**：

```
datapath/
├── aoa2dproc/          ← 到达角2D处理
├── cfarproc/           ← CFAR处理
├── dopplerproc/        ← 多普勒处理
└── rangeproc/          ← 距离处理
```

**kernel/ 内核类别**：

```
kernel/
├── dpl/                ← 驱动平台库
│   └── dpl_demo/
└── freertos/           ← FreeRTOS示例
    ├── posix_demo/
    └── task_switch/
```

#### 第5层：硬件平台标识（固定）

```
xwrL684x-evm/           ← AWRL6844评估板专用标识
```

**规律总结**：
- ✅ 所有MMWAVE_L_SDK固件都包含此标识
- ✅ `xwrL684x` = AWRL6844芯片系列
- ✅ `-evm` = 评估板（Evaluation Module）

#### 第6层：处理器与操作系统配置（4种组合）

| 配置 | 说明 | 适用场景 | 数量占比 |
|-----|------|---------|---------|
| `r5fss0-0_freertos/` | ARM R5F + FreeRTOS | 多任务应用 | ~45% |
| `r5fss0-0_nortos/` | ARM R5F + 裸机 | 简单应用 | ~45% |
| `system_freertos/` | 多核系统 + FreeRTOS | 复杂雷达应用 | ~5% |
| `system_nortos/` | 多核系统 + 裸机 | 高实时性 | ~5% |

**命名解析**：
- `r5fss0-0` = ARM Cortex-R5F SubSystem Core 0
- `system` = 多核协同（R5F + DSP）
- `freertos` = FreeRTOS实时操作系统
- `nortos` = No RTOS（裸机）

#### 第7层：编译器（固定）

```
ti-arm-clang/           ← TI官方ARM编译器（推荐）
```

---

### 1.2 radar_toolbox 目录结构（72个固件）

#### 标准目录层级（5-6层）

```
C:/ti/radar_toolbox_3_30_00_06/
└── source/ti/examples/                 ← 第2层：示例根目录
    └── [应用场景大类]/                 ← 第3层：应用领域
        └── [具体Demo名称]/             ← 第4层：Demo名称
            └── prebuilt_binaries/      ← 第5层：预编译目录
                └── [子型号]/           ← 第6层（可选）：芯片子型号
                    └── [固件文件].appimage
```

#### 第3层：应用场景分类（共6大类）

| 应用场景 | 目录名 | 说明 | 固件数量 |
|---------|--------|------|---------|
| **汽车ADAS** | `Automotive_ADAS_and_Parking/` | 自动驾驶辅助和停车 | ~7 |
| **免提访问** | `Automotive_HandsFree_Access/` | 手势控制开门 | ~2 |
| **车内安全** | `Automotive_InCabin_Security_and_Safety/` | 乘员检测 | ~3 |
| **基础功能** | `Fundamentals/` | 基础示例 | ~12 |
| **工业个人** | `Industrial_and_Personal_Electronics/` | 工业/消费应用 | ~35 |
| **开箱即用** | `Out_Of_Box_Demo/` | OOB演示 | ~5 |

#### 第4层：具体Demo细分

**Automotive_ADAS_and_Parking/**（汽车ADAS）

```
Automotive_ADAS_and_Parking/
├── awrl1432_entry_level_blind_spot_detection/  ← 盲点检测
├── high_end_corner_radar/                      ← 高端角雷达
└── mmwave_2_chip_cascade/                      ← 双芯片级联
```

**Automotive_InCabin_Security_and_Safety/**（车内安全）

```
Automotive_InCabin_Security_and_Safety/
├── AWRL6432_Life_Presence_Detection_Demo_Capon2D/  ← 生命存在检测
├── AWRL6844_InCabin_Demos/                         ← 6844车内Demo ✅
└── Exterior_Intrusion_Monitoring/                  ← 外部入侵监控
```

**Fundamentals/**（基础功能）

```
Fundamentals/
├── CAN_Data_Output/                    ← CAN数据输出
│   ├── 2944_OOB_object_data_over_CAN/
│   └── CAN_Integration/
├── Enabling_PFM_Mode_for_DCDC_Converter/  ← DCDC转换器
├── LIN_Integration/                    ← LIN总线集成
├── nonos_oob/                          ← 无OS开箱即用
├── SBL_Image_Select/                   ← SBL镜像选择
├── SBL_Memory_Initialization/          ← SBL内存初始化
├── xWRL6432_Power_Switching_Modes/     ← 电源切换模式
└── xWRL6432_Raw_Data_Over_SPI/         ← SPI原始数据
```

**Industrial_and_Personal_Electronics/**（工业与个人电子，最多）

```
Industrial_and_Personal_Electronics/
├── 1D_Sensing/                         ← 一维感知
├── Bike_Radar/                         ← 自行车雷达
├── Gesture_Recognition/                ← 手势识别
│   └── xWRLx432_Gesture/
├── Level_Sensing/                      ← 液位感知
│   └── high_accuracy_low_power_sensors/
├── mmWave_Demo/                        ← 毫米波通用Demo
├── Motion_and_Presence_Detection/      ← 运动和存在检测
│   ├── motion_and_presence_2d/
│   └── motion_and_presence_3d/
├── Pose_And_Fall_Detection/            ← 姿态与跌倒检测 ✅
├── Robotics/                           ← 机器人应用
│   ├── Small_Obstacle_Detection/
│   └── surface_classification_ML_flow_pytorch/
├── Smart_Toilet/                       ← 智能马桶
├── True_Ground_Speed/                  ← 真实地速
├── Video_Doorbell/                     ← 视频门铃
└── Vital_Signs/                        ← 生命体征
    └── IWRL6432_Vital_Signs/
```

**Out_Of_Box_Demo/**（开箱即用）

```
Out_Of_Box_Demo/
└── prebuilt_binaries/
    ├── out_of_box_2544.appimage
    ├── out_of_box_2944.appimage
    ├── out_of_box_L1432.appimage
    ├── out_of_box_L6432.appimage
    └── out_of_box_L6432_aop.appimage
```

#### 第5层：预编译目录（固定）

```
prebuilt_binaries/      ← 预编译固件存放目录
```

**规律总结**：
- ✅ radar_toolbox的所有固件都在此目录
- ✅ 无需编译，直接可用
- ✅ 适合快速验证和演示

#### 第6层：芯片子型号（可选）

部分Demo有芯片子分类：

```
prebuilt_binaries/
├── awr2944/            ← AWR2944专用
├── awr2x44p/           ← AWR2x44P系列
├── motion_and_presence_2d/  ← 功能子分类
└── motion_and_presence_3d/
```

---

### 1.3 tools 工具目录（8个固件）

```
C:/ti/radar_toolbox_3_30_00_06/
└── tools/                              ← 工具根目录
    ├── Adc_Data_Capture_Tool_DCA1000_CLI/  ← ADC数据采集
    │   └── prebuilt_binaries/
    │       ├── xWRL1432_motion_and_presence_detection_demo.appimage
    │       ├── xWRL6432_motion_and_presence_detection_demo.appimage
    │       └── xWRL6844_mmwave_demo.release.appimage  ✅
    ├── JTAG_Flasher/                   ← JTAG烧录工具
    │   └── tool/
    │       ├── awr2944/
    │       └── xwrl6432/
    ├── machine_learning_flow/          ← 机器学习流程
    │   └── Jupyter_Notebook/src/4-CCS-Integration/mmwave_dataset_loader/prebuilt_binaries/
    ├── OTA_Swap/                       ← OTA升级
    │   └── prebuilt_binaries/
    └── studio_cli/                     ← Studio命令行工具
        └── prebuilt_binaries/
```

---

## 📋 二、文件命名规律

### 2.1 MMWAVE_L_SDK 文件命名模式

#### 模式A：驱动示例型（最常见，~90%）

```
格式: [驱动名]_[功能描述].[版本].appimage

示例:
├── gpio_led_blink.release.appimage
├── uart_echo.release.appimage
├── i2c_read.release.appimage
├── mcan_loopback_interrupt.release.appimage
└── qspi_flash_dma_transfer.release.appimage
```

**命名组成**：
1. **驱动名**：`gpio`, `uart`, `i2c`, `mcan`, `qspi` 等
2. **功能描述**：`led_blink`, `echo`, `read`, `loopback` 等
3. **版本标识**：`.release` 或 `.debug`
4. **扩展名**：`.appimage`

#### 模式B：系统级固件（~5%）

```
格式: [功能]_system.release.appimage

示例:
├── hello_world_system.release.appimage
├── empty_system.release.appimage
├── uart_echo_system.release.appimage
├── edma_chain_transfer_system.release.appimage
└── ipc_notify_echo_system.release.appimage
```

**命名特征**：
- ✅ 包含 `_system` 后缀
- ✅ 表示多核系统级配置
- ✅ 适用于 `system_freertos/` 或 `system_nortos/` 目录

#### 模式C：数据处理型（~3%）

```
格式: [处理类型]_test_main[_平台].release.appimage

示例:
├── aoa2dproc_test_main.release.appimage
├── cfarproc_test_main_xwrL68xx.release.appimage
├── dopplerproc_test_main.release.appimage
└── rangeproc_test_main_xwrL68xx.release.appimage
```

**命名特征**：
- ✅ 包含 `_test_main` 中缀
- ✅ 可选平台后缀 `_xwrL68xx`
- ✅ 用于信号处理链路测试

#### 模式D：SBL启动固件（~2%）

```
格式: sbl[_变体].release.appimage

示例:
├── sbl.release.appimage
├── sbl_lite.release.appimage
└── spibooting.release.appimage
```

**命名特征**：
- ✅ 以 `sbl` 开头（Secondary Bootloader）
- ✅ 变体：`lite`（精简版）、`spibooting`（SPI启动）
- ✅ 关键固件，启动必需

---

### 2.2 radar_toolbox 文件命名模式

#### 模式E：应用Demo型（最常见，~70%）

```
格式: [功能描述]_demo[_变体].[版本].appimage

示例:
├── motion_and_presence_detection_demo.Release.appimage
├── motion_and_presence_detection_demo_aop.Release.appimage
├── gesture_recognition_demo.Release.appimage
├── pose_and_fall_demo.appimage
├── bike_radar_demo_6432.Release.appimage
└── video_doorbell_demo.Release.appimage
```

**命名组成**：
1. **功能描述**：`motion_and_presence_detection`, `gesture_recognition` 等
2. **Demo标识**：`_demo`
3. **变体**：`_aop`（AOP封装）、`_wcsp`（WCSP封装）、`_6432`（芯片型号）
4. **版本**：`.Release` 或 `.Debug`（首字母大写！）
5. **扩展名**：`.appimage`

#### 模式F：芯片专用型（~15%）

```
格式: [芯片型号]_[功能]_[变体].appimage

示例:
├── awr2944_corner_radar_hecrDSSLink.appimage
├── awr2944_corner_radar_hecrMSSLink.appimage
├── awr2944_corner_radar_hecrMSSLinkCAN.appimage
├── awr2e44p_corner_radar_hecrCAN.appimage
└── demo_in_cabin_sensing_6844_system.release.appimage  ✅
```

**命名特征**：
- ✅ 以芯片型号开头：`awr2944`, `awr2e44p`, `6844`
- ✅ 包含通信接口：`CAN`, `UART`, `LVDS`
- ✅ 包含处理器链接：`DSSLink`, `MSSLink`

#### 模式G：开箱即用型（~8%）

```
格式: out_of_box_[型号][_变体].appimage

示例:
├── out_of_box_2544.appimage
├── out_of_box_2944.appimage
├── out_of_box_L1432.appimage
├── out_of_box_L6432.appimage
└── out_of_box_L6432_aop.appimage
```

**命名特征**：
- ✅ 固定前缀：`out_of_box_`
- ✅ 芯片型号：`2544`, `2944`, `L1432`, `L6432`
- ✅ 可选变体：`_aop`

#### 模式H：工具类型（~5%）

```
格式: [工具名]_[芯片][_变体].[版本].appimage

示例:
├── xWRL1432_motion_and_presence_detection_demo.appimage
├── xWRL6432_motion_and_presence_detection_demo.appimage
├── xWRL6844_mmwave_demo.release.appimage  ✅
├── awr2944_ccsdebug.appimage
└── mmwave_studio_cli_awr2544.appimage
```

**命名特征**：
- ✅ 芯片型号在前：`xWRL6844`, `awr2944`
- ✅ 工具功能描述：`mmwave_demo`, `ccsdebug`, `studio_cli`

#### 模式I：机器学习型（~2%）

```
格式: [功能]_[训练集标识].[版本].appimage

示例:
├── surface_classification_demo_edgeai.Debug.appimage
├── surface_classification_demo_SDK5502-GRASS-NOT.Debug.appimage
├── surface_classification_demo_SDK5502-WET-DRY.Debug.appimage
├── surface_classification_test_SDK5502-GRASS-NOT.Debug.appimage
└── surface_classification_test_SDK5502-WET-DRY.Debug.appimage
```

**命名特征**：
- ✅ 功能前缀：`surface_classification`
- ✅ 训练集标识：`edgeai`, `SDK5502-GRASS-NOT`, `SDK5502-WET-DRY`
- ✅ 区分Demo和Test

---

### 2.3 版本标识规律

#### MMWAVE_L_SDK 版本标识（小写）

```
.release.appimage       ← 发布版本（优化、无调试）
.debug.appimage         ← 调试版本（包含符号）
```

**特征**：
- ✅ 小写 `release` / `debug`
- ✅ 用点号 `.` 分隔
- ✅ 100%使用此格式

#### radar_toolbox 版本标识（首字母大写）

```
.Release.appimage       ← 发布版本
.Debug.appimage         ← 调试版本
.release.appimage       ← 少数情况（工具类）
```

**特征**：
- ✅ 首字母大写 `Release` / `Debug`
- ✅ 用点号 `.` 分隔
- ✅ ~85%使用大写格式
- ✅ 工具类固件可能用小写

---

### 2.4 变体后缀规律

| 后缀 | 含义 | 示例 |
|-----|------|------|
| `_aop` | AOP封装（Always-On Power Domain） | `motion_and_presence_detection_demo_aop.Release.appimage` |
| `_wcsp` | WCSP封装（Wafer-Level Chip-Scale Package） | `motion_and_presence_detection_demo_wcsp.Release.appimage` |
| `_system` | 系统级配置（多核） | `hello_world_system.release.appimage` |
| `_main` | 主程序/测试入口 | `gpio_led_blink_main.release.appimage` |
| `_6432` / `_6844` / `_2944` | 芯片型号 | `bike_radar_demo_6432.Release.appimage` |
| `_DDM` / `_TDM` | DDM/TDM模式 | `awr2944_mmw_nonos_demo_mssDDM_Debug.appimage` |
| `_CAN` / `_UART` / `_LVDS` | 通信接口 | `awr2944_corner_radar_hecrMSSLinkCAN.appimage` |

---

### 2.5 芯片型号标识规律

#### 在文件名中的芯片标识

**AWRL6844相关**：
```
✅ xWRL6844_*                ← 工具类命名
✅ *_6844_*                  ← 应用Demo命名
✅ *_L6844*                  ← 开箱即用命名（少见）
```

**其他芯片型号**：
```
xwrL68xx / xwrl64xx         ← 6xxx系列通用
xwrl1432 / L1432            ← AWRL1432
xwrl6432 / L6432            ← AWRL6432
awr2944 / 2944              ← AWR2944
awr2544 / 2544              ← AWR2544
awr29xx                     ← AWR29xx系列
am273x                      ← AM273x系列
```

**命名大小写规律**：
- MMWAVE_L_SDK：`xwrL684x`（驼峰，L大写）
- radar_toolbox：`xWRL6844`, `xwrl6432`（混用）

---

## 🎯 三、AWRL6844固件筛选规则

### 3.1 目录路径匹配规则

#### ✅ 必须包含（MMWAVE_L_SDK）

```
/xwrL684x-evm/              ← 官方平台标识（100%匹配）
```

#### ✅ 必须包含（radar_toolbox）

```
路径包含以下任一关键词：
├── /AWRL6844_*/            ← 直接包含6844
├── /*_6844_*/              ← 路径中包含6844
├── /6844/                  ← 子目录包含6844
└── 文件名包含 xWRL6844 或 _6844_ 或 _L6844
```

#### ❌ 必须排除

```
/xwrl1432/                  ← AWRL1432（不兼容）
/L1432/                     ← AWRL1432简写
/xwrl6432/                  ← AWRL6432（不兼容）
/L6432/                     ← AWRL6432简写
/awr2944/                   ← AWR2944（不兼容）
/awr2544/                   ← AWR2544（不兼容）
/awr29xx/                   ← AWR29xx系列（不兼容）
/iwrl6432/                  ← 工业版（接口不同）
```

### 3.2 文件名匹配规则

#### ✅ 优先选择

```
文件名包含：
├── xWRL6844                ← 完整型号
├── _6844_                  ← 中间包含
├── _6844.                  ← 结尾包含
└── xwrL68xx                ← 系列通用（兼容）
```

#### ✅ 可用（通用型号）

```
无特定型号标识，但路径包含 xwrL684x-evm/
例如：
├── hello_world.release.appimage        ← 通用基础固件
├── mmwave_demo.release.appimage        ← 通用雷达Demo
└── gpio_led_blink_main.release.appimage ← 通用驱动示例
```

---

### 3.3 实际筛选示例

#### 从354个固件中筛选AWRL6844固件

**Step 1: 路径筛选（MMWAVE_L_SDK）**

```powershell
包含 "xwrL684x-evm"
结果: 282个固件 ✅（全部可用）
```

**Step 2: 路径筛选（radar_toolbox）**

```powershell
包含 "AWRL6844" 或 "_6844_" 或 "xWRL6844"
结果: 2个固件
├── demo_in_cabin_sensing_6844_system.release.appimage  ✅
└── xWRL6844_mmwave_demo.release.appimage  ✅
```

**Step 3: 排除不兼容型号**

```powershell
排除 "L1432", "L6432", "1432", "6432", "2944", "2544", "29xx"
保留结果: 284个AWRL6844可用固件
```

---

## 📊 四、统计数据分析

### 4.1 固件数量分布

| SDK | 固件数量 | 占比 | 6844专用 |
|-----|---------|------|---------|
| MMWAVE_L_SDK_06_01_00_01 | 282 | 79.7% | 282 ✅ |
| radar_toolbox_3_30_00_06 | 72 | 20.3% | 2 ✅ |
| **合计** | **354** | **100%** | **284** |

### 4.2 功能类别分布（MMWAVE_L_SDK）

| 类别 | 数量 | 占比 |
|-----|------|------|
| 驱动示例（drivers/） | ~240 | 85% |
| 数据处理（datapath/） | ~8 | 3% |
| 内核示例（kernel/） | ~8 | 3% |
| 基础示例（hello_world等） | ~8 | 3% |
| 控制示例（control/） | ~4 | 1% |
| 雷达Demo（mmw_demo/） | ~2 | 1% |
| 系统配置（system/） | ~12 | 4% |

### 4.3 操作系统配置分布

| 配置 | 数量 | 占比 |
|-----|------|------|
| r5fss0-0_freertos | ~127 | 45% |
| r5fss0-0_nortos | ~127 | 45% |
| system_freertos | ~14 | 5% |
| system_nortos | ~14 | 5% |

### 4.4 版本标识分布

| 版本 | 数量 | 占比 |
|-----|------|------|
| .release / .Release | ~330 | 93% |
| .debug / .Debug | ~20 | 6% |
| 无版本标识 | ~4 | 1% |

### 4.5 文件名长度分布

| 长度范围 | 数量 | 典型示例 |
|---------|------|---------|
| <30字符 | ~50 | `hello_world.release.appimage` |
| 30-50字符 | ~200 | `gpio_led_blink_main.release.appimage` |
| 50-70字符 | ~80 | `motion_and_presence_detection_demo.Release.appimage` |
| >70字符 | ~24 | `lin_external_responder_interrupts.release.appimage` |

---

## 💡 五、最佳实践建议

### 5.1 固件选择优先级

#### 优先级1：官方SDK（MMWAVE_L_SDK）
- ✅ 稳定性高
- ✅ 文档完善
- ✅ 覆盖全面
- 📁 路径：`MMWAVE_L_SDK_06_01_00_01/examples/`

#### 优先级2：工具箱（radar_toolbox）
- ✅ 功能丰富
- ✅ 即用型Demo
- ✅ 应用场景明确
- 📁 路径：`radar_toolbox_3_30_00_06/source/ti/examples/`

### 5.2 入门推荐路径

**第1步：验证硬件**
```
hello_world.release.appimage
位置: MMWAVE_L_SDK/examples/hello_world/xwrL684x-evm/r5fss0-0_freertos/
```

**第2步：测试外设**
```
gpio_led_blink_main.release.appimage
位置: MMWAVE_L_SDK/examples/drivers/gpio/gpio_led_blink/xwrL684x-evm/r5fss0-0_freertos/
```

**第3步：测试通信**
```
uart_echo.release.appimage
位置: MMWAVE_L_SDK/examples/drivers/uart/uart_echo/xwrL684x-evm/r5fss0-0_freertos/
```

**第4步：雷达功能**
```
mmwave_demo.release.appimage
位置: MMWAVE_L_SDK/examples/mmw_demo/mmwave_demo/xwrL684x-evm/r5fss0-0_freertos/
```

**第5步：应用Demo**
```
demo_in_cabin_sensing_6844_system.release.appimage
位置: radar_toolbox/source/ti/examples/Automotive_InCabin_Security_and_Safety/AWRL6844_InCabin_Demos/prebuilt_binaries/
```

### 5.3 命名规范总结

#### 创建新固件时的命名建议

**基本格式**：
```
[功能模块]_[具体功能]_[变体].[版本].appimage
```

**示例**：
```
radar_tracking_demo_6844.release.appimage
├─ radar_tracking  ← 功能模块
├─ demo            ← 类型标识
├─ 6844            ← 芯片型号
├─ release         ← 版本标识
└─ appimage        ← 扩展名
```

**推荐规则**：
1. ✅ 使用小写字母和下划线
2. ✅ 功能描述清晰简洁
3. ✅ 包含芯片型号（如果特定）
4. ✅ 明确版本标识（release/debug）
5. ✅ 总长度控制在60字符以内

---

## 📋 六、快速查找表

### 6.1 按功能查找固件

| 功能 | 关键词 | 示例固件 |
|-----|--------|---------|
| **Hello World** | `hello_world` | `hello_world.release.appimage` |
| **GPIO测试** | `gpio`, `led` | `gpio_led_blink_main.release.appimage` |
| **串口通信** | `uart`, `echo` | `uart_echo.release.appimage` |
| **CAN总线** | `mcan`, `can` | `mcan_loopback_interrupt.release.appimage` |
| **SPI通信** | `mcspi`, `spi` | `mcspi_loopback_main.release.appimage` |
| **I2C通信** | `i2c` | `i2c_read.release.appimage` |
| **雷达Demo** | `mmwave`, `mmw` | `mmwave_demo.release.appimage` |
| **运动检测** | `motion`, `presence` | `motion_and_presence_detection_demo.Release.appimage` |
| **姿态检测** | `pose`, `fall` | `pose_and_fall_demo.appimage` |
| **车内感知** | `in_cabin`, `6844` | `demo_in_cabin_sensing_6844_system.release.appimage` |
| **SBL引导** | `sbl` | `sbl.release.appimage` |

### 6.2 按目录查找固件

| 目录 | 固件类型 | 路径 |
|-----|---------|------|
| **boot/** | 启动固件 | `MMWAVE_L_SDK/examples/drivers/boot/` |
| **gpio/** | GPIO示例 | `MMWAVE_L_SDK/examples/drivers/gpio/` |
| **uart/** | 串口示例 | `MMWAVE_L_SDK/examples/drivers/uart/` |
| **mcan/** | CAN总线 | `MMWAVE_L_SDK/examples/drivers/mcan/` |
| **hello_world/** | 基础测试 | `MMWAVE_L_SDK/examples/hello_world/` |
| **mmw_demo/** | 雷达Demo | `MMWAVE_L_SDK/examples/mmw_demo/` |
| **AWRL6844_InCabin_Demos/** | 车内感知 | `radar_toolbox/source/ti/examples/Automotive_InCabin_Security_and_Safety/` |
| **Pose_And_Fall_Detection/** | 姿态检测 | `radar_toolbox/source/ti/examples/Industrial_and_Personal_Electronics/` |

---

## 🔍 七、PowerShell筛选脚本

### 7.1 查找所有AWRL6844固件

```powershell
# 读取固件列表
$content = Get-Content "项目文档\2-开发记录\2025-12-17\2025-12-17_TI_SDK固件列表.md"

# 筛选6844固件
$firmware6844 = $content | Where-Object {
    $_ -match "xwrL684x" -or          # MMWAVE_L_SDK标识
    $_ -match "6844" -or              # 直接包含6844
    $_ -match "xWRL6844"              # 工具类标识
} | Where-Object {
    $_ -notmatch "L6432" -and         # 排除6432
    $_ -notmatch "1432" -and          # 排除1432
    $_ -notmatch "2944"               # 排除2944
}

# 输出结果
$firmware6844.Count
$firmware6844 | Out-File "6844固件列表.txt" -Encoding UTF8
```

### 7.2 按功能分类

```powershell
# 分类统计
$categories = @{
    "驱动示例" = $firmware6844 | Where-Object { $_ -match "/drivers/" }
    "基础示例" = $firmware6844 | Where-Object { $_ -match "/hello_world/|/empty/" }
    "雷达Demo" = $firmware6844 | Where-Object { $_ -match "/mmw_demo/|mmwave_demo" }
    "应用Demo" = $firmware6844 | Where-Object { $_ -match "Industrial_and_Personal|Automotive_InCabin" }
    "启动固件" = $firmware6844 | Where-Object { $_ -match "/sbl" }
}

# 显示统计
foreach ($key in $categories.Keys) {
    Write-Host "$key : $($categories[$key].Count) 个"
}
```

---

**总结**：通过分析354个固件文件，我们总结出了TI SDK的目录结构有7层（MMWAVE_L_SDK）和5-6层（radar_toolbox）的标准层级，文件命名有9种主要模式，并提供了完整的AWRL6844固件筛选规则和最佳实践建议。
