# 🎯 AWRL6844固件智能匹配算法改进方案

> **创建日期**: 2025-12-20  
> **问题**：当前智能匹配算法推荐的雷达配置文件与InCabin Demo固件的实际配套配置文件不对应  
> **目标**：改进匹配算法，准确推荐正确的配置文件

---

## 📋 问题分析

### 当前问题

**InCabin Demo固件**（`demo_in_cabin_sensing_6844_system.release.appimage`）应该匹配的配置文件：

```
C:\ti\radar_toolbox_3_30_00_06\tools\visualizers\AWRL6844_Incabin_GUI\src\chirpConfigs6844\
├── cpd.cfg                          ✅ 儿童存在检测（Child Presence Detection）
├── sbr.cfg                          ✅ 安全带提醒（Seat Belt Reminder）
├── intrusion_detection.cfg          ✅ 入侵检测
└── intrusion_detection_LP.cfg       ✅ 入侵检测低功耗版
```

**但当前匹配算法推荐的配置文件**：完全不对应，可能推荐了其他SDK的通用配置。

### 根本原因分析

#### 1. **路径关联性不足**

现有算法主要基于：
- 应用场景关键词匹配（如"车内"、"InCabin"）
- 文件名中的芯片型号（6844）
- 检测距离范围

**缺失**：
- ❌ 未考虑固件和配置文件是否在同一SDK中
- ❌ 未识别特定Demo固件的专用配置目录
- ❌ 未利用GUI工具目录结构关系

#### 2. **配置文件元数据不准确**

配置文件解析可能无法识别：
- 配置文件的实际应用场景（CPD、SBR、入侵检测）
- 配置文件与特定Demo的绑定关系
- GUI工具的配置组织结构

#### 3. **评分权重不合理**

当前评分机制：
```python
# 应用场景匹配：40分
# 芯片型号匹配：20-30分
# 距离范围匹配：10-20分
# 功耗模式匹配：10分
```

**问题**：
- 应用场景匹配权重过高，但匹配规则过于宽泛
- 没有"同SDK加权"或"同目录树加权"
- 缺少"配置文件文件名语义解析"

---

## 🔧 改进方案

### 方案1：基于目录树关系的匹配（推荐）⭐

#### 核心思想

固件和配置文件如果在同一SDK的相关目录下，应该获得**最高优先级**。

#### 算法步骤

```python
def match_configs_for_firmware_v2(firmware: FirmwareInfo) -> List[Tuple[ConfigInfo, float]]:
    """改进的配置文件匹配算法（v2）"""
    matches = []
    
    for config in config_files:
        score = 0.0
        
        # ========== 1. 目录树关系匹配（最高优先级）==========
        # 检查是否在同一Demo目录下
        if is_same_demo_directory(firmware.path, config.path):
            score += 100.0  # 最高优先级
            
        # 检查是否在同一SDK下的关联目录
        if is_related_in_sdk(firmware.path, config.path):
            score += 80.0
            
        # ========== 2. 配置文件名语义匹配 ==========
        # 解析配置文件名的语义
        config_semantics = parse_config_filename(config.filename)
        
        # InCabin Demo特殊处理
        if 'InCabin' in firmware.path or 'incabin' in firmware.path.lower():
            if 'cpd' in config.filename.lower():  # Child Presence Detection
                score += 60.0
            elif 'sbr' in config.filename.lower():  # Seat Belt Reminder
                score += 60.0
            elif 'intrusion' in config.filename.lower():  # Intrusion Detection
                score += 60.0
                
        # ========== 3. 应用场景匹配（降低权重）==========
        if firmware.subcategory and config.application:
            if firmware.subcategory in config.application:
                score += 30.0  # 从40降到30
            elif config.application in firmware.subcategory:
                score += 25.0
                
        # ========== 4. 芯片型号匹配 ==========
        if '6844' in config.filename.lower():
            score += 20.0  # 6844专用
        elif 'awrl' in config.filename.lower():
            score += 15.0  # AWRL系列
            
        # ========== 5. 检测距离合理性 ==========
        if config.range_m > 0:
            if config.range_m <= 10 and is_short_range_app(firmware):
                score += 10.0
            elif 10 < config.range_m <= 50:
                score += 5.0
                
        # ========== 6. 功耗模式匹配 ==========
        if 'low_power' in firmware.path.lower() and '_LP' in config.filename:
            score += 10.0
            
        matches.append((config, score))
    
    matches.sort(key=lambda x: x[1], reverse=True)
    return matches
```

#### 关键函数实现

##### 1. 同Demo目录判断

```python
def is_same_demo_directory(fw_path: str, cfg_path: str) -> bool:
    """判断固件和配置是否在同一Demo目录下
    
    示例：
    固件：C:\\ti\\radar_toolbox_3_30_00_06\\source\\ti\\examples\\
         Automotive_InCabin_Security_and_Safety\\AWRL6844_InCabin_Demos\\
         prebuilt_binaries\\demo_in_cabin_sensing_6844_system.release.appimage
    配置：C:\\ti\\radar_toolbox_3_30_00_06\\tools\\visualizers\\
         AWRL6844_Incabin_GUI\\src\\chirpConfigs6844\\cpd.cfg
    
    判断依据：
    1. 路径中都包含"InCabin"或"incabin"
    2. 都在同一radar_toolbox版本下
    3. 配置在visualizers/GUI工具目录下
    """
    fw_lower = fw_path.lower()
    cfg_lower = cfg_path.lower()
    
    # 提取SDK根目录
    fw_sdk = extract_sdk_root(fw_path)
    cfg_sdk = extract_sdk_root(cfg_path)
    
    if fw_sdk != cfg_sdk:
        return False
    
    # InCabin Demo特殊规则
    if 'incabin' in fw_lower:
        if 'incabin_gui' in cfg_lower or 'awrl6844_incabin' in cfg_lower:
            return True
            
    # 其他Demo规则
    # ... 根据其他Demo的目录结构添加规则
    
    return False

def extract_sdk_root(path: str) -> str:
    """提取SDK根目录名称
    
    示例：
    C:\\ti\\radar_toolbox_3_30_00_06\\... → radar_toolbox_3_30_00_06
    C:\\ti\\MMWAVE_L_SDK_06_01_00_01\\... → MMWAVE_L_SDK_06_01_00_01
    """
    path_parts = path.replace('\\', '/').split('/')
    
    for part in path_parts:
        if 'radar_toolbox' in part.lower():
            return part
        if 'mmwave_l_sdk' in part.lower():
            return part
        if 'radar_academy' in part.lower():
            return part
            
    return ""

def is_related_in_sdk(fw_path: str, cfg_path: str) -> bool:
    """判断固件和配置是否在同一SDK的关联目录
    
    关联规则：
    - examples目录下的固件 → tools/visualizers下的配置
    - examples目录下的固件 → tools/mmwave_data_recorder下的配置
    """
    fw_lower = fw_path.lower()
    cfg_lower = cfg_path.lower()
    
    # 同一SDK
    if extract_sdk_root(fw_path) != extract_sdk_root(cfg_path):
        return False
    
    # 固件在examples，配置在tools
    if 'examples' in fw_lower and 'tools' in cfg_lower:
        return True
        
    return False
```

##### 2. 配置文件名语义解析

```python
def parse_config_filename(filename: str) -> Dict[str, str]:
    """解析配置文件名的语义
    
    示例：
    cpd.cfg → {'scene': 'child_presence_detection', 'power': 'normal'}
    intrusion_detection_LP.cfg → {'scene': 'intrusion', 'power': 'low_power'}
    xWRL6844_4T4R_tdm.cfg → {'antenna': '4T4R', 'mode': 'tdm', 'chip': '6844'}
    """
    semantics = {}
    filename_lower = filename.lower()
    
    # 应用场景识别
    scene_keywords = {
        'cpd': 'child_presence_detection',
        'sbr': 'seat_belt_reminder',
        'intrusion': 'intrusion_detection',
        'vital': 'vital_signs',
        'gesture': 'gesture_recognition',
        'occupancy': 'occupancy_detection'
    }
    
    for keyword, scene in scene_keywords.items():
        if keyword in filename_lower:
            semantics['scene'] = scene
            break
    
    # 功耗模式识别
    if '_lp' in filename_lower or 'low_power' in filename_lower:
        semantics['power'] = 'low_power'
    else:
        semantics['power'] = 'normal'
    
    # 天线配置识别
    if '4t4r' in filename_lower:
        semantics['antenna'] = '4T4R'
    elif '2t4r' in filename_lower:
        semantics['antenna'] = '2T4R'
    
    # TDM/BPM模式
    if 'tdm' in filename_lower:
        semantics['mode'] = 'tdm'
    elif 'bpm' in filename_lower:
        semantics['mode'] = 'bpm'
    
    # 芯片型号
    if '6844' in filename_lower:
        semantics['chip'] = '6844'
    elif '6843' in filename_lower:
        semantics['chip'] = '6843'
    
    return semantics

def is_short_range_app(firmware: FirmwareInfo) -> bool:
    """判断是否为短距离应用（≤10m）
    
    短距离应用关键词：
    - InCabin（车内）
    - Indoor（室内）
    - Gesture（手势）
    - Vital Signs（生命体征）
    """
    short_range_keywords = [
        'incabin', 'indoor', 'gesture', 'vital', 
        'occupancy', '车内', '室内', '手势'
    ]
    
    fw_text = (firmware.path + firmware.subcategory).lower()
    
    return any(keyword in fw_text for keyword in short_range_keywords)
```

---

### 方案2：基于配置文件首行注释的匹配

#### 核心思想

许多配置文件的首行包含说明性注释，可以解析这些注释获取语义信息。

#### 示例分析

```bash
# cpd.cfg开头
% Radar front-end parameters
sensorStop 0
channelCfg 15 15 0

# 可以添加更明确的注释
% Application: Child Presence Detection (CPD)
% Compatible Firmware: demo_in_cabin_sensing_6844_system.release.appimage
% SDK: Radar Toolbox 3.30.00.06
```

#### 实现建议

```python
def parse_config_metadata(config_path: str) -> Dict[str, str]:
    """从配置文件中解析元数据（前20行注释）"""
    metadata = {}
    
    try:
        with open(config_path, 'r', encoding='utf-8', errors='ignore') as f:
            for i, line in enumerate(f):
                if i >= 20:  # 只读前20行
                    break
                    
                line = line.strip()
                
                # 解析注释行
                if line.startswith('%') or line.startswith('#'):
                    # Application: xxx
                    if 'application' in line.lower():
                        metadata['application'] = extract_value_after_colon(line)
                    # Compatible Firmware: xxx
                    elif 'firmware' in line.lower():
                        metadata['compatible_firmware'] = extract_value_after_colon(line)
                    # SDK: xxx
                    elif 'sdk' in line.lower():
                        metadata['sdk'] = extract_value_after_colon(line)
                        
    except Exception:
        pass
        
    return metadata
```

---

## 📊 新评分机制

### 评分权重分配

| 匹配类型                   | 权重 | 说明                                     |
| -------------------------- | ---- | ---------------------------------------- |
| **同Demo目录**       | 100  | 固件和配置在同一Demo项目下（最强关联）   |
| **同SDK关联目录**    | 80   | 在同一SDK的examples和tools关联目录       |
| **配置文件名语义匹配** | 60   | 文件名明确指示应用场景（cpd、sbr等）     |
| **应用场景文本匹配**   | 30   | 固件子类别与配置应用场景文本匹配         |
| **芯片型号匹配**       | 20   | 配置文件名包含6844                       |
| **检测距离合理性**     | 10   | 配置距离与固件应用场景匹配               |
| **功耗模式匹配**       | 10   | 低功耗固件匹配_LP配置                    |

### 预期结果

#### InCabin Demo固件匹配示例

**固件**：`demo_in_cabin_sensing_6844_system.release.appimage`

**Top 3推荐配置**：

1. **cpd.cfg** - 总分：200
   - 同Demo目录：100
   - 同SDK关联：80
   - 文件名语义（cpd）：60
   - 应用场景：30
   - 6844芯片：20
   - 短距离：10

2. **sbr.cfg** - 总分：200
   - 同Demo目录：100
   - 同SDK关联：80
   - 文件名语义（sbr）：60
   - 应用场景：30
   - 6844芯片：20
   - 短距离：10

3. **intrusion_detection.cfg** - 总分：200
   - 同Demo目录：100
   - 同SDK关联：80
   - 文件名语义（intrusion）：60
   - 应用场景：30
   - 6844芯片：20
   - 短距离：10

---

## 🔧 实施步骤

### Step 1：修改匹配器类

修改 `awrl6844_firmware_matcher.py` 中的 `match_configs_for_firmware` 方法。

### Step 2：添加辅助函数

在匹配器类中添加：
- `is_same_demo_directory()`
- `is_related_in_sdk()`
- `extract_sdk_root()`
- `parse_config_filename()`
- `is_short_range_app()`
- `parse_config_metadata()`（可选）

### Step 3：单元测试

创建测试用例验证：
```python
def test_incabin_demo_matching():
    fw = FirmwareInfo(
        path="C:\\ti\\radar_toolbox_3_30_00_06\\source\\ti\\examples\\"
             "Automotive_InCabin_Security_and_Safety\\AWRL6844_InCabin_Demos\\"
             "prebuilt_binaries\\demo_in_cabin_sensing_6844_system.release.appimage",
        filename="demo_in_cabin_sensing_6844_system.release.appimage",
        subcategory="车内安全检测"
    )
    
    matcher = AWRL6844FirmwareMatcher()
    matches = matcher.match_configs_for_firmware(fw)
    
    # 验证Top 3
    assert 'cpd.cfg' in matches[0][0].filename
    assert 'sbr.cfg' in matches[1][0].filename
    assert 'intrusion_detection.cfg' in matches[2][0].filename
```

### Step 4：更新GUI显示

确保新的匹配结果正确显示在智能匹配标签页。

---

## 📝 后续改进方向

### 1. 配置文件元数据标准化

建议在所有配置文件顶部添加标准化元数据注释：

```cfg
% ============================================
% Configuration Metadata
% ============================================
% Application: Child Presence Detection
% Compatible Firmware: demo_in_cabin_sensing_6844_system.release.appimage
% SDK: Radar Toolbox 3.30.00.06
% Chip: AWRL6844
% Antenna: 4T4R
% Range: 0-5m
% Power Mode: Normal
% ============================================
```

### 2. 机器学习增强

未来可以考虑：
- 收集用户实际使用的固件-配置组合
- 使用协同过滤算法优化推荐
- 基于固件二进制特征提取进行匹配

### 3. 配置文件内容解析

深度解析配置文件的参数内容：
- chirpComnCfg 参数
- frameCfg 参数
- channelCfg 参数
- 检测算法参数

根据参数值判断配置的适用场景。

---

## 🎯 总结

### 核心改进点

1. ✅ **引入目录树关系匹配**（最高优先级100分）
2. ✅ **配置文件名语义解析**（60分）
3. ✅ **降低宽泛文本匹配权重**（从40降到30）
4. ✅ **增加同SDK关联目录判断**（80分）

### 预期效果

- InCabin Demo固件将正确推荐cpd.cfg、sbr.cfg等配套配置
- 其他固件的配置推荐准确性也会显著提升
- 用户体验更好，减少手动查找配置的时间

### 版本计划

- **v1.6.0**：实施方案1的基本功能
- **v1.7.0**：添加配置文件元数据解析（方案2）
- **v2.0.0**：引入机器学习增强推荐

---

> **文档作者**: AI Assistant  
> **审核**: 待项目负责人审核  
> **实施**: 待开发实施
