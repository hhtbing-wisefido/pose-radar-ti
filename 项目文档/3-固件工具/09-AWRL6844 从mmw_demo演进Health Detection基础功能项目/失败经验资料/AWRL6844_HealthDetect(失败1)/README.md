# AWRL6844 Health Detection Project

**基于第3章三层架构的完整重建项目**

---

## 🎯 项目目标

实现AWRL6844雷达健康检测功能，采用MSS-DSS双核协作架构：
- **MSS/R5F**: 主控、配置、输出
- **DSS/C66x**: 信号处理、特征提取（40%利用率）

---

## 📁 项目结构

```
AWRL6844_HealthDetect/
├── src/
│   ├── common/                 # 共享接口层 (MSS+DSS)
│   │   ├── shared_memory.h     # L3 RAM内存映射
│   │   ├── data_path.h         # DPC结构定义
│   │   ├── mmwave_output.h     # TLV输出格式
│   │   └── health_detect_types.h # 特征数据结构
│   │
│   ├── mss/                    # MSS应用层 (R5F @ 200MHz)
│   │   ├── health_detect_main.c/h   # 主控程序
│   │   ├── dpc_control.c/h          # DPC协调
│   │   ├── cli.c/h                  # CLI命令
│   │   ├── presence_detect.c/h      # 存在检测
│   │   ├── tlv_output.c/h           # TLV输出
│   │   └── radar_control.c/h        # 雷达控制
│   │
│   ├── dss/                    # DSS算法层 (C66x @ 450MHz)
│   │   ├── dss_main.c/h             # DSP入口
│   │   ├── feature_extract.c/h      # 特征提取
│   │   └── dsp_utils.c/h            # DSP工具
│   │
│   └── system/                 # 🔴 系统配置（必须）
│       ├── linker_mss.cmd           # MSS链接脚本
│       ├── linker_dss.cmd           # DSS链接脚本
│       ├── shared_memory.ld         # 共享RAM定义
│       ├── system.xml               # 🔴 多核协调配置
│       ├── makefile_system_ccs_bootimage_gen # 🔴 打包脚本
│       ├── config/
│       │   ├── metaimage_cfg.release.json # 🔴 Release配置
│       │   └── metaimage_cfg.debug.json   # 🔴 Debug配置
│       └── README.md
│
├── mss_project.projectspec     # CCS MSS项目配置
├── dss_project.projectspec     # CCS DSS项目配置
├── system_project.projectspec  # 🔴 CCS System项目配置（必须）
├── BUILD_GUIDE.md              # 编译指南
└── README.md                   # 本文件
```

---

## 🏗️ 三层架构设计

### Layer 1: Common (共享接口层)

**职责**: MSS与DSS之间的数据结构和内存映射

**关键文件**:
- `shared_memory.h` - L3 RAM 896KB划分：
  - `DPC_CONFIG_BASE` (4KB): MSS→DSS配置
  - `POINT_CLOUD_BASE` (8KB): DSS→MSS点云
  - `FEATURE_DATA_BASE` (4KB): DSS→MSS特征
  - `DPC_RESULT_BASE` (4KB): DSS→MSS统计
  - `SCRATCH_BUFFER_BASE` (876KB): DSS内部使用

### Layer 2: MSS (应用层)

**职责**: 主控制、配置管理、数据输出

**模块**:
1. **health_detect_main** - BIOS任务框架、初始化
2. **dpc_control** - 通过IPC触发DSS处理
3. **cli** - UART命令行接口
4. **presence_detect** - 高层特征分析
5. **tlv_output** - TLV格式UART输出
6. **radar_control** - mmWave API封装

### Layer 3: DSS (算法层)

**职责**: 信号处理、特征提取（40% DSP利用率）

**模块**:
1. **dss_main** - DSP主循环、IPC消息处理
2. **feature_extract** - 点云特征提取算法：
   - 质心计算 (centerX/Y/Z)
   - 空间分布 (spreadX/Y/Z)
   - 速度分量 (velocityX/Y/Z)
3. **dsp_utils** - Cache操作、周期计数

---

## 🔄 数据流程

```
Radar ADC
    ↓
[DSS] 1D FFT (Range)
    ↓
[DSS] 2D FFT (Doppler)
    ↓
[DSS] CFAR Detection
    ↓
[DSS] DOA Estimation → Point Cloud
    ↓
[DSS] Feature Extraction → Features ⭐ (新功能)
    ↓
    Write to L3 Shared RAM
    ↓
[MSS] Read Point Cloud & Features
    ↓
[MSS] Presence Detection
    ↓
[MSS] TLV Output → UART
```

---

## 🆚 与mmw_demo对比

| 方面 | mmw_demo | AWRL6844_HealthDetect |
|-----|----------|----------------------|
| **代码来源** | SDK原始代码 | 从零重建 |
| **架构** | 扁平结构 | 三层架构 |
| **DSS使用** | 0% (仅MSS) | 40% (特征提取) |
| **目录结构** | source/ | common/mss/dss/ |
| **特征提取** | ❌ 无 | ✅ 有 (DSS实现) |
| **代码关系** | 参考学习 | 重新实现 |

**重要**: 本项目**参考**了mmw_demo的功能和API用法，但代码是**全新编写**，不是复制粘贴！

---

## 🔧 编译说明

### 为什么MSS和DSS分别编译？

**双核异构架构**：

AWRL6844包含两个**完全不同**的处理器：

| 核心 | MSS | DSS |
|-----|-----|-----|
| **CPU** | ARM Cortex-R5F @ 200MHz | C66x DSP @ 450MHz |
| **指令集** | ARM指令集 | TI C6000指令集 |
| **编译器** | TI ARM Compiler (armcl) | TI C6000 Compiler (cl6x) |
| **二进制格式** | ARM COFF/ELF | C6000 COFF/ELF |

**结论**: ❌ 不可能用一个编译器编译两种不同的CPU架构！

### ⭐ 系统打包（🔴 必须实现）

**正式编译方式**：

```
项目配置：
├─ mss_project.projectspec       # MSS项目
├─ dss_project.projectspec       # DSS项目
└─ system_project.projectspec    # 🔴 系统项目（必须）

CCS操作：
└─ Build System Project
    ├─ 自动编译MSS → mss.rig
    ├─ 自动编译DSS → dss.rig
    └─ 打包成 .appimage (单文件)

生成文件：
└─ health_detect_system.release.appimage

烧录：
└─ UniFlash一次烧录 .appimage

优点：
  ✅ 一次烧录
  ✅ 版本一致
  ✅ 完整发布
  ✅ 包含RF固件

状态：🔴 必须完成，不是可选的
```

**系统打包配置文件**：
- ✅ `system_project.projectspec` - 系统项目配置
- ✅ `src/system/system.xml` - 多核协调配置
- ✅ `src/system/makefile_system_ccs_bootimage_gen` - 打包脚本
- ✅ `src/system/config/metaimage_cfg.release.json` - MetaImage配置

### 辅助方式：分别编译（仅供调试）

**仅用于快速调试**：

```
CCS操作：
├─ Build MSS Project → MSS.out
├─ Build DSS Project → DSS.out
└─ UniFlash分别烧录

优点：✅ 快速调试、单独更新
缺点：❌ 两次烧录、版本管理困难
适用：开发调试 ✅（辅助手段）
```

**⚠️ 重要说明**：
- 🔴 **系统打包是必选项，不是可选的**
- 🔴 **最终交付必须是.appimage格式**
- ✅ 分别编译仅作为开发调试的辅助手段
- ✅ Milestone 2必须验证系统打包成功

### .appimage文件结构

```
health_detect_system.release.appimage
├─────────────────────────────────
│ Meta Header (元信息)
│  ├─ Image Type: Multi-Core
│  └─ Security: GP (General Purpose)
├─────────────────────────────────
│ MSS Image (mss.rig)
│  ├─ MSS.out (ARM代码)
│  ├─ Load Address: 0x00000000
│  └─ Size: ~500KB
├─────────────────────────────────
│ DSS Image (dss.rig)
│  ├─ DSS.out (DSP代码)
│  ├─ Load Address: 0x00800000
│  └─ Size: ~200KB
├─────────────────────────────────
│ RF Firmware Patch
│  └─ mmwave_rfs_patch.rig
└─────────────────────────────────
```

### 启动流程

```
上电 → ROM Bootloader
      ↓
   读取.appimage
      ↓
   加载MSS Image到MSS内存
      ↓
   加载DSS Image到DSS内存
      ↓
   加载RF Firmware
      ↓
   启动R5F (主核)
      ↓
   R5F通过IPC唤醒C66x (从核)
      ↓
   双核并行运行
```

---

## 🚀 快速开始

### 1. 环境准备

**必需**:
- Texas Instruments Code Composer Studio (CCS) 10.0+
- TI mmWave SDK 3.6.2+
- ARM Compiler 20.2.7.LTS
- C6000 Compiler 8.3.12

### 2. 导入项目

```bash
# 在CCS中
File → Import → CCS Projects
Browse到: D:\7.project\TI_Radar_Project\project-code\AWRL6844_HealthDetect
选择: AWRL6844_HealthDetect_MSS, AWRL6844_HealthDetect_DSS
```

### 3. 配置SDK路径

编辑 `mss_project.projectspec` 和 `dss_project.projectspec`:

```xml
<pathVariable name="TI_SDK_ROOT" 
              path="C:/ti/mmwave_sdk_03_06_02_00_00" 
              scope="project"/>
```

改为你的实际SDK路径。

### 4. 编译

```
右键 AWRL6844_HealthDetect_MSS → Build Project
右键 AWRL6844_HealthDetect_DSS → Build Project
```

**预期结果**: 
- ✅ 生成 `Debug/AWRL6844_HealthDetect_MSS.out`
- ✅ 生成 `Debug/AWRL6844_HealthDetect_DSS.out`
- ✅ 0 errors (warnings可接受)

详细步骤见 [BUILD_GUIDE.md](BUILD_GUIDE.md)

---

## 📊 当前状态

### ✅ 已完成

**架构层面** (100%):
- ✅ 三层目录结构
- ✅ L3 RAM内存映射
- ✅ DPC数据结构
- ✅ TLV输出格式

**MSS模块** (100%框架):
- ✅ health_detect_main - BIOS框架、初始化
- ✅ dpc_control - DPC协调框架
- ✅ cli - CLI命令表
- ✅ presence_detect - 状态机完整实现
- ✅ tlv_output - TLV封装完整实现
- ✅ radar_control - API封装框架

**DSS模块** (100%框架):
- ✅ dss_main - DSP入口框架
- ✅ feature_extract - 特征提取完整算法
- ✅ dsp_utils - Cache/周期计数

**CCS项目** (100%):
- ✅ mss_project.projectspec
- ✅ dss_project.projectspec
- ✅ BUILD_GUIDE.md

### ⏳ 待完成（TODO标记）

**关键集成**:
- [ ] mmWave API实际调用 (radar_control.c)
- [ ] IPC mailbox实现 (dpc_control.c, dss_main.c)
- [ ] BIOS配置文件 (.cfg)
- [ ] ADC数据路径配置

**这些TODO不影响编译验证！**

---

## 🎯 里程碑

### ✅ Milestone 1: 架构重建 (已完成)
- 目标: 按第3章架构从零重建代码
- 状态: **完成** (2026-01-07)
- 成果: 所有文件创建完成，代码全新编写

### 🔄 Milestone 2: 编译验证 (进行中)
- 目标: CCS编译0错误
- 任务:
  - [ ] 导入CCS项目
  - [ ] 配置SDK路径
  - [ ] 编译MSS和DSS
  - [ ] 解决链接问题

### ⏳ Milestone 3: 功能实现 (待开始)
- 目标: 完成TODO，实现完整功能
- 任务:
  - [ ] mmWave API集成
  - [ ] IPC通信实现
  - [ ] ADC数据路径
  - [ ] BIOS配置

### ⏳ Milestone 4: 硬件测试 (待开始)
- 目标: 在AWRL6844 EVM上验证
- 任务:
  - [ ] 固件烧录
  - [ ] 雷达启动验证
  - [ ] 点云数据输出
  - [ ] 特征提取验证

---

## 📚 参考文档

**本项目参考**:
- TI mmWave SDK 示例: `mmw_demo/`
- InCabin Demo: 多核架构参考
- 第3章演进架构文档

**学习路径**:
1. 阅读 `mmw_demo/source/dpc/dpc.c` → 理解DPC工作流程
2. 阅读 `mmw_demo/source/mmw_cli.c` → 理解CLI命令
3. 阅读 `mmw_demo/source/mmwave_demo.c` → 理解主控逻辑
4. 参考本项目实现 → 按三层架构重写

---

## 🔧 开发规范

### 代码风格

- **C语言标准**: C99
- **命名规范**:
  - 模块前缀: `HealthDetect_`, `DPC_`, `TLV_`
  - 私有函数: `static`
  - 全局变量: `g`前缀
- **注释**: 所有文件头部标注参考来源

### 文件头模板

```c
/**
 * @file xxx.c
 * @brief 模块功能简述
 * 
 * Reference: mmw_demo参考文件
 * Adapted for: 三层架构用途
 */
```

---

## 🐛 已知问题

1. **TODO标记**: 代码中包含TODO需要完成
2. **未测试**: 当前仅框架代码，未在硬件测试
3. **库依赖**: 需要正确配置mmWave SDK库路径

---

## 📝 版本历史

### v1.0.0 (2026-01-07)
- ✅ 完整架构重建
- ✅ 所有模块框架完成
- ✅ CCS项目配置创建
- 🎯 目标: 验证编译可行性

---

## 🤝 贡献指南

**当前阶段**: 内部开发，暂不接受外部贡献

**下一步计划**:
1. 完成编译验证
2. 实现TODO部分
3. 硬件测试验证
4. 文档完善

---

## 📧 联系方式

项目负责人: [Your Name]  
技术问题: [Issue Tracker]

---

> 💡 **重要提醒**: 这是从零重建的项目，不是mmw_demo的修改版！
> 
> - 参考了mmw_demo的**功能**和**API用法**
> - 代码是**全新编写**，符合第3章三层架构
> - 实现了mmw_demo没有的**DSS特征提取**功能
